"""OPTIONAL LLM-judge scorer for P1 transcripts — STUB, SHIPS DISABLED.

The P1 deliverable is reviewable transcripts; the judgment is the maintainer's, by
design (brief). If ever enabled (judge.enabled: true in schedules.yaml), this
would score each arm's final state for coverage / faithfulness / specificity
against the source — but it is intentionally not implemented so the pilot
cannot silently substitute machine judgment for the pre-registered manual review.
"""

raise SystemExit("P1 judge is disabled by design; review the transcripts manually.")
