# Handoff — 2026-07-21 (post-chain-gate, pre-gauntlet-arbitration)

## Read these first, in order

1. `experiments/e2prime/STAGEA-CHAIN-GATE-RESULTS.md` — the verdict, per-seed tables, §5 attribution, all probe findings, instrument corrections. The authoritative record.
2. `experiments/e2prime/STAGEA-EXPLORATION-LEDGER.md` — the lever board and the operating mode rules for post-gate exploration.
3. `docs/GAUNTLET-STAGEA-2026-07-21.md` — the review prompt the maintainer is currently running through 10+ external models.
4. Raw artifacts: `runs/stageA_chain_gate/` (per-seed JSON with full prompts/outputs/snapshots, manifests with content hashes, `ANALYSIS.txt`, `diagnostics.log`, `_companion/`, `_probe*/`).

## Where we stand

**The Stage-A chain gate ran and returned an ARCHITECTURE GATE: student 0/10, floor 0/10** (floor inside its expected 0–1 band, so the pre-committed absolute thresholds hold; verdict survives hand review — zero candidates either arm, no linked 23→988 pair at close anywhere). Per E2PRIME-CRITERION §3: no further training spend on THIS instantiation (flat text state + ADD/REPLACE/REMOVE + this schedule + 1.5B). The single-retry rule does NOT apply at 0/10 — a "retry" is only licensed from 2–4/10. Modified instantiations under fresh dated pre-regs are the legitimate path (that IS the criterion's ≤1/10 next move).

Everything is committed on `e2prime-prep` (NOT pushed): `54c59e8` (pre-run addendum + evaluator) → `9807509` (closing budget) → `ba6da78` (analysis) → `1208e80` (paraphrase false-negative fix) → `2e005aa` (companion/diag/probe tools) → `6aa5853` (truncation-cause fix) → `6c8cdd2` (Tier-1 metric) → `c28b43a` (op-following metric) → `f0da40a` (results record) → `67b6823` (query-first probe) → `d12ad54` (padded probe + ledger) → this handoff + gauntlet prompt. Working tree clean.

## The findings that should drive next-step reasoning

- **Training moved every component; only the end-to-end conjunction is 0=0.** Student vs floor: applied ops/pass 7.0 vs 2.4; state 7.7K vs 0.7K tok; table named 7/10 vs 2/10; mapping recorded 3/10 vs 0/10; operands held 2/10 vs 0/10; clean-state compose 1/10 vs 0/10. "Trained > untrained" is established and is a wanted data point.
- **RETAIN is clean** (0 losses despite ~150 strict-guard blocks). The D31 clobber story is dead as the failure mechanism.
- **RECORD fails by all-or-nothing whole-table transcription** (24/24 or 0/24, never the needed row) **or fabricated range summaries** ("70 entries" for a 24-entry table).
- **CLOSE fails two dissociable ways** (padded probe, 2026-07-21): distractor content misdirects retrieval (decoy capture: seed 2 answered 86 off `CARRIER_FUEL_BP` while holding both operands; with distractors→neutral filler it lands on the right table), and **length itself kills two-operand compose at 1.5B** (full-length neutral state: single-value answers only; the chain fired only in a 2-line distilled state → 2353). Confirms the maintainer's small-model long-context-degradation hypothesis specifically for compose.
- **Not promptable:** retrieve-first instruction 0/10; question-first order 0/10 (close already data-first — consistent with the Google data>prompt finding the maintainer cited; the UNTESTED half is the recording passes, which currently run instructions-first, the anti-pattern).
- **GATE-1 PASS** (unguessable fixture), **GATE-3**: clean-state compose 1/10 student / 0/10 floor → a 1.5B compose ceiling is plausible; scale confound named and open.
- 6/10 student closings emitted no `ANSWER:` line — format compliance is its own small-model failure; next pre-reg should carry a secondary prose-number metric.

## THE MAINTAINER'S WORKING MODE — internalize this, it governs everything

- Self-described **ML novice doing AI-assisted "vibe research"**, double-checks everything but knows both they and the AI can get it wrong. Much of the methodology (criterion, gates, bars) is AI-drafted, maintainer-checked. Treat instruments with humility — this session corrected its own scorer 4×, once for a paraphrase false-negative (`23→988` vs `23: 988`) that would have manufactured the architecture gate from a bug. Hand-read raw outputs before trusting any flag.
- **Process, not one-shot.** test → (retrain/modify architecture) → test → repeat. The project only "fails" at a wall of no improvement — and the ledger operationalizes that: each instantiation pre-names the component rates it must beat. **Improvement > perfection; trained>untrained is itself a wanted result.**
- **Gates and pre-reg keep it scientific; probes keep it moving.** Oracle/forcing probes are fine when labelled non-verdict (`gate_legal:false`) and never pooled with a gate. Negative probes get recorded, not discarded.
- Wants **honesty over polish**; corrections stated plainly (this session self-corrected in-channel three times; that was well received). No flattery. Realistic endgame, in their words: "I've exhausted my expertise and got the project as far as I can without input from real ML researchers" — and the artifact trail is built to make that a presentable deliverable, not a shrug.
- **The multi-model gauntlet is their standing validation tool**: 10+ models/harnesses review the same self-contained prompt; hallucinations get caught by cross-checking reviewers against each other and against the repo. Prior art: `docs/gauntlet-arbitration.md` (June) — reviews → arbitration with independently verified citations → verdict table → doc revisions.

## What happens next (your job, new session)

1. The maintainer returns with **gauntlet results** (10+ model reviews of `docs/GAUNTLET-STAGEA-2026-07-21.md`, answers numbered 1–10).
2. **Arbitrate, don't aggregate.** Follow the gauntlet-arbitration.md pattern: every checkable reviewer claim gets verified against repo ground truth (`runs/stageA_chain_gate/`, the results doc) or independent search before it influences anything. Reviewers WILL hallucinate numbers, prior art, and "obvious flaws" — a confident claim that contradicts archived data loses to the data. Prior-art citations should be verified before they kill or spare a lever (the June arbitration caught a mis-cited arXiv ID).
3. Synthesize into a **revised lever board** (update `STAGEA-EXPLORATION-LEDGER.md` statuses; add new levers with credit to which reviewers surfaced them; record kills with reasons).
4. Then **draft the fresh pre-reg** for whichever instantiation(s) the maintainer picks — fresh dated bars, component-rate targets vs the Stage-A baseline (results doc §5 table), one lever per instantiation where affordable, declared confounds where not.
5. Ask before your first commit. Nothing is pushed or published without maintainer review (standing rule). Scrub rule stands: the machine username and the SSH host alias never enter committed files.

## Ready-to-fire $0 probes (GPU idle, both need only frozen weights)

- **Lever 3:** data-first recording-prompt order on the untrained floor (no train/eval mismatch there). If signal → the retrain path is re-rendering the existing 5,730 teacher rows in the new order (zero new teacher spend, one A5000 run).
- **Levers 4/5:** selective/guided-reading harness prototypes on the frozen student.

## Instrument debts (before ANY next battery — ledger has details)

Port the paraphrase-robust matcher from `analyze_chain_gate.py` into the runner's live flags; archive `usage.completion_tokens` per call; add the secondary prose-number closing metric.

## Environment quick-notes

- Repo: `C:\Users\Boz\pass-conditioned-reading`, branch `e2prime-prep`. Models: student `runs/stageA_coupled_v2/stageA_v2-f16.gguf`, floor `models/Qwen2.5-1.5B-Instruct-f16.gguf`, llama.cpp b9222 at `models/llamacpp/llama-server.exe` (symlink to `/d/llm/bin/`). Serving: one model at a time, ctx 24576, `-ngl 99`, ports 8097–8102 used so far. ~7.5 min/draw student, ~2 min floor.
- Run analysis: `python experiments/e2prime/analyze_chain_gate.py` (set `PYTHONIOENCODING=utf-8` — states contain `→`, cp1252 console crashes otherwise). Probes: `probe_closing.py --arm student --variant <asis|distilled|padded|modules|retrieve|budget|queryfirst>`. Companion: `stagea_companion_checks.py`. All refuse nothing — they are non-verdict; only `stagea_chain_gate.py` carries the addendum commit gate.
- Memory file `project_disco_context_diffusion.md` is current through the verdict; this handoff supersedes it on detail.
