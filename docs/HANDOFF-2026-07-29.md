# Handoff — 2026-07-29 (the $0 queue, run)

## Read these first, in order

1. `experiments/e2prime/HOME-AVAILABILITY-RESULTS.md` — **the session's decisive document.** The
   archive scan and the P1f own-scaffold probe. Leads with the plain-language summary.
2. `experiments/e2prime/STAGEA-EXPLORATION-LEDGER.md` — lever board, revised tonight. Lever 5
   demoted; two new demand repairs (1a salience, 1b binding survival).
3. `docs/MNL-TYPE-A-FIXTURE-DRAFT.md` — **needs your edit.** The type-A fixture, the decoy design,
   and the pre-registered decision wording, with three open questions marked for you.
4. Background unchanged: `docs/HANDOFF-2026-07-28.md`, `experiments/e2prime/P1-PROBE-RESULTS.md`
   (now carrying a partial-supersession banner), `docs/gauntlet-arbitration-2026-07-28.md`.

## One-paragraph state of play

The four cheap queue items are run. The headline of 07-28 — "home availability is the recording
variable" — **does not survive contact with non-oracle states, and the correction is more useful
than the original.** The student's own Phase-A pass already builds homes (adder recorded at pass 4
in 8/11 archived draws, 8/10 on fresh seeds, untrained 7B 10/10), and then fills them **0/10**,
while the hand-written `ctrl8` state is filled 5/10. Reading the raw draws says why: the pass-26
tile is **94% auto-composed helper boilerplate and 5.8% the module that matters**, and the
REPLACE-into-the-matching-entry policy spends its whole budget faithfully recording the 94%. The
oracle state worked because its other entries were inert — it removed the competition, which no
real scaffold can do. So the blocker at RECORD is **salience, not home availability**: the model
is not failing to record, it is recording the wrong bulk, and "record exact values, names and
quotes" genuinely describes the filler. Separately, a clean within-model contrast isolates a
second blocker: a standalone scalar survives Phase A 8/11, the same number as one row of a 24-row
table survives **1/11** (two-sided p≈7.5×10⁻³) — the table is compressed to its range,
`24 surcharge rates (10-988 cents)`, and a lookup table compressed to its range cannot answer a
lookup. The owed confirming run passed: relation-fill 4/10 → 3/10 at fresh seeds (p=1.00).

## What ran, what it cost

All $0, frozen weights, one model at a time on the 1080 Ti.

| item | instrument | wall |
|---|---|---|
| archive scan (non-oracle home availability) | `scan_home_availability.py` (new, no GPU) | seconds |
| P1f own-scaffold fill + fresh-seed confirm | `stagea_p1_probes.py --probe p1f --seeds 11-20` | 448s + 420s |
| M-NL G4 capability ladder | `experiments/mnl/g4_capability_ladder.py` (new) | see §G4 |

## Findings (denominators inline, Fisher two-sided, hand-read before believing)

- **Phase-A builds homes unprompted.** Adder `1365` first recorded at **k=4 in 8/11** archived
  student draws (floor 0/11), under the scaffold rubric that says not to chase specifics; still in
  the final state 6/11. Fresh-seed probe: dedicated home 8/10, value 5/10; untrained 7B home 10/10,
  value 0/10.
- **And does not fill them.** Own-scaffold fill acquisition **0/10** (student) with 8/10 dedicated
  homes present, vs **5/10** into the oracle `ctrl8` state. The no-home arm acquired **3/10** — via
  ADD, because it had nothing to REPLACE. The untrained 7B is 0/10 on its own coarse scaffold too
  (its other own-scaffold arm collapsed on op-id format, `REPLACE 15` not `REPLACE S15`, and is
  reported as **inconclusive** per the Q4/protocol-transfer rule).
- **Why:** pass-26 tile = 2,981 tok, of which `fulfillment/line_quote.py` is **172 (5.8%)**. A
  scaffold covering every module matches the other 94% perfectly.
- **Extraction granularity, the second blocker.** Scalar 8/11 vs table-row binding **1/11**,
  p≈7.5×10⁻³. The digits `988` reach the state 5/11 — as the range maximum, not as region 23's
  value.
- **Edit-budget waste, non-oracle:** of 1,252 Phase-B REPLACEs, **322 (26%) rewrote an entry to
  byte-identical text**; **121 (10%) clobbered** an unrelated entry. Floor never REPLACEs (99% ADD)
  — the REPLACE bias is trained-in. Hand-read find: `student_s7` at pass 12 destroyed the one entry
  holding both the surcharge range and the adder, at the pass showing the surcharge table.
- **Confirming run passed.** ctrl8 relation 4/10 (s1–10) → 3/10 (s11–20), p=1.00 → **replicated
  mechanism** under the three-tier rule. Adder 9/10 → 5/10, p=0.14 — quote it as ~5–9/10, not 9/10.
- **Relation-writing is still training-only** (7B 0/10 in every cell at fresh seeds, including the
  oracle cell it fills 6/10). Inverse-GATE-3 replicates.

## Consequences for the plan

- **Lever 5 demoted** from lead training lever to necessary-not-sufficient. Home-building already
  happens; it is not the binding constraint. The next probe on that line is a **salience** probe,
  not another home probe.
- **Two demand repairs added** to the ledger's prerequisite list: **1a** the recording demand must
  distinguish load-bearing content from bulk (or the fixture must stop being 94% filler — measuring
  through that ratio measures the ratio); **1b** key→value bindings must survive compression.
- **The P7 relevance fork is now the RECORD joint's live blocker,** with a mechanism attached
  rather than as a loose end.
- **Lever 13 (budget policy) gains support:** 26% of the Phase-B edit budget buys literally nothing.
- Phase 2 sequencing is unchanged, but the fixture rebuild now has a hard requirement it did not
  have this morning: the signal-to-filler ratio is an experimental variable, not scenery.

## M-NL groundwork (drafted, awaiting your edit)

- `docs/MNL-TYPE-A-FIXTURE-DRAFT.md` — the "keeps checking arXiv" arc as 9 carrier turns with a
  ledger of mechanical checks, a **surface-matched decoy** (same 9 link-pastes, checks follow
  completed work, experiments finish — so the decoy holds the surface feature and drops the
  emergent one), the G3 local control, and DRAFT 0/1/2 decision wording. **Three open questions
  are marked for you** (§4): where the bar sits, n rollouts for R1's baseline distribution, and
  whether G3 scores on the same output.
  - One authoring constraint worth knowing: earlier drafts had late turns like "is it weird that I
    feel sick opening arXiv". Those are removed on purpose — any turn that names the feeling makes
    the signal detectable from a single slice and **fails G1**.
- `experiments/mnl/g4_capability_ladder.py` — the R2 capability gate as a scale ladder
  (1.5B→3B→7B→14B, API-ready). Component flags are archived separably so re-scoring under your
  signed wording needs no re-run.

### G4 ladder — first results (instrument smoke-test; fixture and rubric UNSIGNED)

n=10 per rung, temp 0.7, carrier turns only. Pre-filter counts, hand-read confirmed.

| rung | makes the integrated call | notes "nothing finished" | centres the latest paper |
|---|---|---|---|
| Qwen2.5-1.5B f16 | **0/10** | 2/10 | 9/10 |
| Qwen2.5-3B f16 | **0/10** | 3/10 | 8/10 |
| Qwen2.5-7B Q4 | **0/10** | 1/10 | 1/10 |
| Qwen2.5-14B Q4 | **0/10** | 5/10 | 6/10 |

**Nothing local passes G4 on this draft fixture — the whole ladder is 0/10**, and `bounded` and
`pattern_named` are 0/10 at *every* rung. Per R2 that points the M-NL arms at API models, which is
affordable (the experiment is inference-only) and is exactly the outcome R2 was written to catch
before the arms burned a comparison on it.

There is a scale gradient, but only on the objective half: noticing that nothing is finished rises
2→3→1→5 of 10. Acting on it does not. The 14B's best draw opens *"Given the status of the project
and the work items that are still open…"* and then schedules all three items across four tidy
weeks — the observation is used as a scheduling preamble, never as a signal.

Two things make the zero stronger than it looks:

- The evidence set **states outright** "None of them has been marked finished." The zero-completions
  fact is *given*, not inferred, and the models still do not act on it. `qwen7b` seed 6 labels the
  ablation `Status: Ongoing since Week 1` and treats it as a scheduling detail.
- Asked to plan a month for a project whose evidence shows prior-art checking displacing execution,
  `qwen7b` seed 6 schedules **more prior-art comparisons** ("Focus on the remaining comparisons
  with 'Scheduled Context Refinement' and 'Progressive Compression for Agent Memory'").

Two things make it weaker, and both are yours to close:

- **The fixture and the 0/1/2 wording are my drafts, not your pre-registration.** A different
  authoring, or a task framed slightly differently, may well elicit the call. The component flags
  are archived separately so you can re-score without re-running anything.
- G2 (the shape doc's oracle-ceiling gate) has **not** run. Until a large model demonstrably makes
  this call on this fixture, "small models fail" and "the target is too subtle" are not separated —
  the same confound R2 exists to prevent. Your lived anecdote is the informal version of G2; the
  formal one is one API call away and should run before anyone reads these zeros as a scale result.

**Environment note:** Qwen2.5-14B-Q4 (8.4 GB) does not fit comfortably on the 1080 Ti at
`--ctx 8192 --ngl 99` — `llama-server` exceeded the 300 s health timeout. At `--ctx 4096` it loads
and generates but at ~8 min/draw (10.9 GB used, thrashing). For a real 14B rung use partial offload
or run it off-box.

## Instrument debts

- **PAID:** non-oracle home-availability measurement; own-scaffold fill; the fresh-seed confirming
  run; per-seed (paired) state builders in the probe harness; acquisition-vs-carryover scoring.
- **OUTSTANDING (unchanged):** paraphrase-robust matcher into the chain runner's live flags;
  archive `usage.completion_tokens` per call; P1c `--enforce-budget` arm; dual scoring in the CHAIN
  runner.
- **NEW:** `post_metrics.n_target_home` counts a bare mention in a module list as a home. The
  dedicated-vs-mention distinction was made in analysis, not in the metric; fold it in before
  anyone quotes `n_target_home` on its own.

## Environment

- Repo `C:\Users\Boz\pass-conditioned-reading`, branch `e2prime-prep`. **Nothing committed this
  session — awaiting your go, per the standing rule.** Working tree carries: 2 new tools, 2 new
  docs, edits to `stagea_p1_probes.py`, `P1-PROBE-RESULTS.md`, `STAGEA-EXPLORATION-LEDGER.md`.
- New artifacts (untracked): `runs/stageA_chain_gate/_probe_p1/p1f_{student,qwen7b}.json`,
  `.../home_availability_scan.json`, `runs/stageA_chain_gate/p1f.log`, `runs/mnl_g4/`,
  `runs/mnl_g4.log`.
- `PYTHONIOENCODING=utf-8` mandatory. Scrub rule unchanged.
- Probe runner gained `--probe p1f`; builders are now `(seed, rec)` so a cell can pair per seed.
  The four existing probes were regression-checked on `--dry-run` after the signature change.

## Your job, next session

1. **Edit the M-NL fixture draft** — the three open questions gate the pre-registration.
2. **Decide the salience probe.** The obvious $0 cell: re-run the pass-26 fill with a recording
   demand that names load-bearing content, against the model's own scaffold, and see whether
   acquisition moves off 0/10. That is a one-cell addition and it tests demand repair 1a directly.
3. P1c still needs `--enforce-budget` in `stagea_chain_gate.py`.
4. Phase-2 fixture repair when you say go — now with the filler-ratio requirement.
