# Handoff - 2026-07-20

## Current status

Stage-A training run 2 is complete and clears the E2' P-C training-health gate:

- Artifact: `runs/stageA_coupled_v2/stageA_v2-f16.gguf`.
- In-envelope P-C result: validity `0.9699` and op-type agreement `0.7606` on 213 held-out rows.
- Bars: validity >= 0.95 and agreement >= 0.75. Verdict: PASS.
- The 109 out-of-envelope rows are an appendix only: validity `0.8346`, agreement `0.3211`.
- Run 1 remains the retained audit trail for the silent `keep_start` truncation failure.

The next scientific action is the Stage-A P7 chain gate, not another training run.

## Gates already satisfied

- P-A: strict v3.2 content-addressed edit harness is implemented and replay-validated.
- P-B: relevance-control v2 landed Branch 3. The floor failed the positive control at structure altitude, so query-conditioned framing is mandatory for every E2' recording gate.
- P-C: passed by run 2, as above.

The chain-gate rule remains frozen: ten sampled draws at temperature 0.7 (seeds 1-10) are the verdict denominator, with a separate temperature-0 seed-42 anchor. Student >=5/10 demonstrates the trained chain; 2-4/10 permits exactly one declared retry; <=1/10 is the architecture gate for this text-state/edit-op instantiation.

## Local execution is feasible

The user prefers local execution. It was directly checked on the GTX 1080 Ti:

- GPU before load: 9,883 MiB free of 11,264 MiB.
- `runs/stageA_coupled_v2/stageA_v2-f16.gguf` served successfully in llama.cpp at 24,576 context and full GPU offload.
- Loaded GPU use: 5,290 MiB, leaving 5,817 MiB free.
- llama.cpp: version 9222 (`9a532ae4b`).
- Same-base floor is already local: `models/Qwen2.5-1.5B-Instruct-f16.gguf`.
- The temporary server was stopped after the check.

The frozen model-free P7 verifier also passed all guards: source presence, dense-floor exclusion, Phase-A scaffold coverage, split Phase-B fragments, and relevance-control-v2 content checks. It confirms `compute_line_surcharge(23) = 988 + 1365 = 2353`.

## Uncommitted change

`experiments/e2prime/STAGEA-CHAIN-GATE-ADDENDUM.md` is a new, untracked file. It records maintainer approval and freezes the operational implementation of the chain gate:

- P7 fixture v2 (seed 70707), frozen scaffold and schedule.
- strict v3.2 state application with a 24-op cap.
- trained and floor f16 GGUF paths, local 24K serving, one model at a time.
- query-conditioned need class: operational questions about pricing, order flow, configuration, and cross-module behavior; no target identifier, location, or value is named.
- archive and manual-adjudication requirements.

The user explicitly approved this package. However, do **not** run a chain-gate model draw until the addendum is committed: its own status line and the E2' preregistration amendment rule require it to be committed before the affected run. The next agent must obtain explicit user authorization before making a Git commit; do not assume this approval includes committing.

No evaluator code was successfully written and no chain-gate model outputs exist.

## Important implementation constraint

Do not reuse `experiments/p7_split_unit/run.py` directly. It uses the legacy P7 grammar and legacy `IntegrationState()` default guard, while the Stage-A student was trained against the E2' v3.2 grammar and `IntegrationState(guard="strict")`. Reusing it would confound the gate.

Create a separate evaluator, suggested path: `experiments/e2prime/stagea_chain_gate.py`.

It should:

1. import the v3.2 prompt components from `pca.teacher_gen` (`GRAMMAR`, `RUBRICS`, `_stage`) and state/parser from `pca.editops`;
2. load fixture v2, `scaffold_struct.json`, and P7 turns; assert fixture values, source coverage, scaffold coverage, and Phase-B split before serving;
3. build the P7 coupled passes using `pca.planted.build_schedule`, replacing Phase-A views with frozen scaffold views;
4. run with strict state application and persist per-pass system/user prompts, raw model output, parsed ops, invalid lines, guard reports, snapshots, and state text;
5. run the final question from final state alone, with an explicit generic permission to combine values only when the state contains the relation;
6. write resumable per-seed JSON records plus a manifest containing hashes of fixture, source, scaffold, runner, addendum, and model;
7. keep the ten sampled draws separate from the deterministic anchor and mark machine-detected successes as requiring hand verification.

Use the exact query framing recorded in the addendum. It is consistent with `E2PRIME-CRITERION.md` and Branch 3, and avoids the P7 legacy neutral/specifies framing.

## Required follow-on checks

After the chain battery, retain the preregistered companion checks in the final Stage-A record:

- relevance-control v2 on the trained student;
- GATE-1 backfill re-run;
- GATE-3 seeded-compose re-run;
- P3-rubric prompt-floor reference on the same untrained base.

They are diagnostics/reference rows. They cannot relax the chain-gate verdict.

## The stale `DRAFT` labels

`docs/E2PRIME-PREREG.md` and `docs/E2PRIME-CRITERION.md` still say `DRAFT for maintainer sign-off` and describe a pre-training state. Their history shows they were committed before teacher-data generation (initial commit `5391edb`, followed by pre-data amendments). By their own rule, they are binding for this run.

The new addendum records the user's sign-off and says the header wording is historical/stale. Prefer an additive dated status record rather than rewriting the old documents as if their original draft-time statements were made after training.

## Tooling note

The nested `tools.apply_patch` helper stalled in this session. The successful fallback was still the required Codex patch mechanism: invoke the local Codex executable directly with `--codex-run-as-apply-patch` and the full patch as one `ProcessStartInfo.ArgumentList` item. Do not use shell redirection or direct file writes.

## Suggested resume sequence

1. Read this handoff and `experiments/e2prime/STAGEA-CHAIN-GATE-ADDENDUM.md`.
2. Ask the user for explicit authorization to commit the addendum; commit only after that authorization.
3. Implement the separate strict-v3.2 evaluator and run static/model-free validation.
4. Run one non-verdict smoke draw only if its status is documented as such; otherwise go directly to the frozen battery after the committed addendum.
5. Serve student and floor sequentially on the local 1080 Ti at 24K context, preserving all raw artifacts.
6. Hand-verify any candidate chain successes before reporting a verdict.
