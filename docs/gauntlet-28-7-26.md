opus-4.6-agy

  1. Fix the fixture first. The fixture tested a joint (COMPOSE over three premises) that no training row, rubric, or teacher policy ever built. Fix the fixture — specifically, expose the relation in ≥3 passes and add ANSWER format to training. This is
  highest-information-per-spend because until you have a fixture that can be passed, any training run is uninterpretable. The representation change (placeholders) is worth testing but only within a valid fixture. A fixture fix costs ~$0 in compute and
  ~days of design time. Confidence: high.

  2. I favour the structural explanation (long prefix consumes attention budget) over training artifact or rubric wording, but not by much. The control-schema condition (different schema, still suppresses) argues against training artifact. The rubric
  explanation ("integrate into existing entries") is plausible but the step-not-slope shape is wrong for it — if it were a rubric cue, 25 vs 155 entries should change the model's interpretation of "integrate." The cheapest discriminator: rewrite the
  rubric to explicitly say "ADD new entries for content not yet represented" and rerun the pass-26 probe with 25 entries vs empty. If the rubric swap eliminates the step, rubric wins. If it persists, it's structural. Cost: ~20 inference calls, zero
  training. Confidence: medium. The caveat that empty-state 7/10 uses malformed ops the guard would block weakens the entire measurement — the step could partly be "the model knows it should REPLACE when state exists but ADD when it doesn't, and the
  ADD rubric was never taught." You should also test with a 3B model, which has more attention headroom.

  3. "Early passes emit typed slots, later passes fill them" is sound in principle but will break at the REPLACE-validation boundary. The guard requires exact-quote content-addressing for REPLACE. A placeholder 23: <cents> can be replaced, but the
  model must quote it exactly. If the placeholder text doesn't match what the model generates as a quote, the guard blocks the fill. What breaks first: the guard rejects fills because placeholder wording drifts from what the model emits as quotes.
  Mitigation: make placeholder syntax rigid and machine-parseable ([TBD:cents] rather than natural-language descriptions), and add a guard fallback that matches placeholder type-tags, not just verbatim quotes. This is a tractable engineering problem,
  not a conceptual flaw. Confidence: medium-high.

  4. Yes, hard-cap the state. 1.92× budget means the model never learned to compress, and at 69% of context the model's attention to new content is mechanically starved (this likely contributes to finding 7). Eviction policy: LRU by last-modified pass,
  with a merge-before-evict step — when an entry hasn't been touched in N passes, offer it for consolidation into a parent entry before dropping it. Cost: some information loss, but the current uncapped regime already produces information loss
  (suppression of new content). Start with hard rejection of ADD ops when budget is exceeded; this is simpler than eviction and directly testable. Confidence: medium. The risk is that hard caps produce a different failure mode (thrashing), but the
  current unbounded growth is clearly worse.

  5. (c) No training — rebuild the fixture and re-measure first. Justification: findings 1–4 collectively show the E1 training data is unsalvageable (wrong rubric, wrong close format, no placeholders, no relation tracking, lossy teacher). Any training
  run on repaired data requires a repaired fixture to evaluate against, and you don't yet have one. The suppression effect (finding 7) could invalidate the placeholder approach before you spend GPU-hours. Sequence: (1) resolve suppression via rubric
  experiment ($0), (2) rebuild fixture ($0), (3) measure untrained 3B floor on new fixture (~$0), (4) then do a single 3B training run on repaired data. If forced to pick between (a) and (b): (a), 3B, because Fisher p≈5×10⁻⁴ on compose is decisive.
  Confidence: high.

  6. Yes, retrieval over the state at close is legitimate. The constraint is "source discarded, answer from the state alone." BM25/embedding retrieval over state entries is selective attention over the model's own work product — it's equivalent to the
  model re-reading its notes before answering. It doesn't reintroduce discarded source. The oracle-filtered result (1.5B produces 2353) proves the information exists in the state; the bottleneck is finding it within a 155-entry, 7,692-token state. This
  is a real engineering solution, not a rule violation. Caveat: if retrieval makes the close trivial regardless of state quality, it could mask bad recording — always test alongside a state-quality metric. Confidence: high.

  7. Rule: a sporadic success is evidence of latent capability only if it replicates at ≥3/10 (p<0.05 against a uniform-random baseline over the answer space) on a fresh pre-registered draw set. Below that, treat it as sampling noise until proven
  otherwise. The P7 cross-family data illustrates this: Llama-70B's 1/10 compose and Qwen-72B's 2/10 are indistinguishable from noise given the answer space. The 3B's 9/10 on seeded compose is not sporadic — that's signal. The project should adopt a
  minimum of 3/10 on a fresh, pre-registered n=10 draw as the threshold for "latent capability worth investigating." Confidence: high.

  8. Hallucination check. Two things look too convenient: (a) The cross-family recombination data (21/22 fail to compose) is presented as proving "text-state recombination is fundamentally hard," but the test used untrained models on a format (text
  state + closing prompt) none of them were trained for — this conflates "hard for an untrained model on an unfamiliar protocol" with "hard in principle." (b) The DISCO-NATIVE-ARCHITECTURE document's claim that content-addressed string ops "strip
  relational bindings" is stated as fact but is actually an interpretation — the 1.5B trained model's 1/10 vs 3B untrained's 9/10 on seeded compose suggests scale, not representation format, is the binding. One factual concern: the document says "0 of
  28 archived final states ever recorded the relation" — but the rubric never asked for relations, so this is expected behavior, not evidence of architecture failure. The finding is real but the interpretation (architecture can't do it) is unsupported.
  Confidence: medium-high.

  9. Strongest honest claim: "A pass-conditioned coarse-to-fine reading schedule with content-addressed state operations is a viable protocol skeleton for multi-pass document integration. An untrained 3B can compose from a clean 8-entry state at 9/10.
  The first training run tested an unpassable fixture and tells us nothing about architecture capability." What's missing: one end-to-end success on a valid fixture. Without that, the protocol is a design, not a result. The nuance experiment (the
  actual goal) remains unrun. Confidence: high.

  10. Steelman for rebuild from scratch: The fixture was unpassable, the training data was generated with a rubric that never requested relations, the close format was never in training, the health check excluded the only stage the verdict depends on,
  52% of verbatim rows were silently dropped by the length gate, the teacher was lossy 61.4% of the time, the state budget was unenforced, and the suppression effect may be structural. Every component of the pipeline — fixture, rubric, close format,
  health check, length gate, teacher policy, budget enforcement — failed independently. Patching each one is six separate fixes to a pipeline that produced zero signal. The probability that all six patches interact correctly on the first try is low. A
  ground-up rebuild with a proper forward process, 3B base, and enforced budget would take the same calendar time as patching and would avoid inheriting hidden assumptions. Do I buy it? Partially. The evaluation fixture should absolutely be rebuilt
  from scratch — it's small and its flaws are fundamental. The training pipeline should be rebuilt with repaired rubrics, close format, and budget enforcement, but the infrastructure (teacher_gen, editops, guard, schedule) is sound engineering that
  doesn't need replacement. The architecture (multi-pass, content-addressed ops, compression schedule) has no evidence against it yet — it was never validly tested. Scrap the fixture and training data; keep the protocol infrastructure; test before
  training. Confidence: medium-high.

  11. The diffusion correspondence.

  (a) The constructed forward process (blank gold states into typed placeholders) will produce valid training pairs, but compression's determinism and semantic selectivity do break the core mechanism that makes diffusion's reverse process learnable. In
  standard diffusion, noise is i.i.d. and the model learns a denoising score function that depends only on the current noise level, not the specific corruption path. Compression is neither random nor path-independent in the same sense — blanking 23:
  988 to 23: <cents> destroys different information than blanking 1365 to <adder>, and the optimal fill depends on which values were blanked, not just "how many." This is closer to masked language modeling with structured masks than diffusion. The
  constructed pairs are valid supervised data — the question is whether the model can generalize across blanking patterns the way diffusion models generalize across noise samples. I think it can work as a data-generation strategy (it solves the
  teacher-rollout cost problem) but the learning dynamics won't have diffusion's convergence guarantees. Confidence: medium.

  (b) The phase A/B mixture does break the framing. Phase A (decreasing compression of the whole document) is denoising-like — same content at increasing resolution. Phase B (new tiles at varying compression) is coverage expansion — new information,
  not refinement. Mixing them means the "noise level" isn't monotonically decreasing; it resets when a new tile appears. They should be separated, with Phase A producing a scaffold of typed placeholders and Phase B filling them. This is already the
  proposed fix and it's well-motivated. Confidence: high.

  (c) Cheapest falsifying measurement: generate 50 (blanked-state, gold-state) pairs at a fixed blanking level, train the 3B on them for one epoch, and measure fill accuracy on held-out pairs at the same level. If fill accuracy is near chance (the
  model can't learn to fill typed placeholders from compressed views even with direct supervision), the correspondence is dead. If it works at one level but fails when you mix levels, the phase-separation hypothesis is confirmed but the correspondence
  survives in weakened form. Cost: ~hours on your A5000. Regarding prior art: MDLMs (Sahoo et al., ICML 2024) and MDLM variants mask tokens uniformly at random; your corruption is semantically structured and entry-level rather than token-level, and
  conditioned on an external signal (the view) that MDLMs lack. The genuinely different thing is external conditioning — your "denoising" step receives new information from outside the state, whereas standard text diffusion conditions only on the
  corrupted state itself. This is more like conditional generation (image diffusion conditioned on text) than unconditional denoising. Confidence: medium.

---

gemini-3.1-pro-agy

  1. Fix the fixture. (Confidence: High)
  The cheapest and most informative change is modifying the teacher's rubric/prompt to explicitly demand capturing formulas and relations, not just "values, names and quotes." If Claude Haiku 4.5 cannot extract the relation when explicitly commanded to
  do so in the fixture, no amount of training or representation tweaking will save the student. Fix the task definition before fixing the architecture.
  2. Rubric effect. (Confidence: High)
  The rubric pushes a REPLACE-biased policy. With a populated state, the model attempts to map the new relation to existing entries. Finding no logical target (because it's a new relation, not an update to a stored value), and discouraged from ADDing,
  it drops the fact. With an empty state, REPLACE is impossible, so it defaults to malformed ADDs.
  Cheapest experiment: Run the exact same pass-26 tile and 25-entry state, but swap the rubric for one that explicitly commands: "If a fact does not fit existing entries, you MUST use ADD." If the recording rate recovers, it is a rubric effect.
  3. It moves the failure. (Confidence: Medium)
  "Early passes emit typed empty slots" shifts the burden from retaining information to predicting the document's schema from heavily compressed early views. What breaks first: the model will fail to allocate slots for highly specific mechanisms (like
  compute_line_surcharge) because they are abstracted away in early compressions. Later passes will have no slots to REPLACE into, recreating the exact same failure mode.
  4. Yes, hard-cap with explicit model eviction. (Confidence: High)
  An unbounded state defeats the premise of fixed-context limits.
  Policy: Do not use automatic LRU; the model must explicitly REMOVE or overwrite entries to free space. Add a BUDGET_REMAINING token.
  Cost: It forces the model to perform triage (which consumes reasoning tokens and requires joint valuation of old vs. new facts). It will likely collapse performance initially until the model learns to compress its own state.
  5. (c) No training — rebuild the fixture and re-measure first. (Confidence: High)
  Your teacher policy failed to record the relation in 100% of archived cases (0/28). 61.4% of teacher events are lossy summaries. Distilling a student on 5,730 rows of fundamentally broken behavior will only yield a faster broken model. Do not burn
  your single A5000 run overfitting to failure. Fix the teacher prompts, prove the teacher can score 10/10, then train.
  6. No, it is illegitimate. (Confidence: High)
  The stated goal is preserving nuance and answering "from the final state alone." A query-aware retrieval step turns the state into a passive database and shifts the reasoning burden to the final closing step. The hypothesis is that the model can
  maintain an active, integrated understanding across passes. If you use BM25 at the end, you are just building standard RAG over an expensive, lossy summarization pipeline.
  7. Rule: Causal counterfactuals. (Confidence: Medium)
  A success is a latent capability if you can isolate a single variable (e.g., rubric instruction, state sparsity) that reliably shifts the success rate across multiple random seeds with statistical significance. It is sampling noise if no structural
  intervention reliably replicates it. Adopt this rule: if you cannot trigger the success on demand by changing the environment, treat it as noise.
  8. Yes, Fisher p-value. (Confidence: Low)
  The math 1365 + 988 = 2353 is correct. However, your reported Fisher exact test p-value for 9/10 vs 1/10 is ≈5×10⁻⁴. A two-tailed Fisher exact test for a 2x2 contingency matrix of [9, 1] and [1, 9] yields p ≈ 0.0011. A one-tailed test yields 0.00054.
  You are quoting a one-tailed p-value for what should be a two-tailed hypothesis (since you didn't firmly pre-register that 3B must be better than trained 1.5B). Otherwise, the claims are internally consistent.
  9. Strongest honest claim: (Confidence: High)
  "We have built a bounded, content-addressed memory loop for reading long documents, but models currently fail to integrate relational logic into it without explicit instruction."
  Missing thing: A working teacher policy. The project is irrelevant until you prove that any model (even an oracle teacher) can successfully execute the desired state-maintenance strategy end-to-end.
  10. Steelman the rebuild: (Confidence: High)
  Argument: The current pipeline clones a single, fragile teacher trajectory that fails to capture relations 100% of the time. The REPLACE-bias and sequential accretion are completely misaligned with how information actually unfolds in text. Patching
  the rubric won't fix the fact that sequential reading doesn't naturally produce an integrated state. You must rebuild using the constructed synthetic forward process: take perfect gold states, progressively blank them into placeholders, and train the
  model to reverse this. This guarantees path-independence, perfect teacher data, and forces the model to learn targeted refinement rather than just lossy summarization.
  Do I buy it? Yes. The current dataset is 5,730 rows of poison. Patching it leaves you fighting the teacher's latent biases. A constructed forward process forces the exact structural behavior you want by definition.
  11. The diffusion correspondence. (Confidence: Medium)
  (a) It is a post-hoc metaphor, and the constructed forward process will struggle. In image diffusion, Gaussian noise is semantically uniform. Text compression is semantically destructive but highly biased. You cannot "denoise" a summary into a
  specific exact quote unless the specific quote is perfectly preserved in the summary, which defeats the purpose of compression. It is closer to inpainting from a reference context than true diffusion.
  (b) Yes, the mixture breaks the framing. Diffusion operates homogeneously across the entire latent space at timestep t. Mixing global refinement (Phase A) with sequential chunking (Phase B) means the "noise" distribution is structurally different
  depending on k. They must be separated, or k becomes a meaningless variable.
  (c) Cheapest falsification: Take a gold final state, apply the "synthetic forward process" to blank out exactly one highly specific noun, provide the heavily compressed Phase-A view of that document, and ask the model to restore the noun. If it
  hallucinated a plausible but incorrect noun, the correspondence is false because the forward process destroyed the information required for the reverse process. Text diffusion requires the "noise" to be reversible; lossy compression is not.

---

deepseek-v4-pro-freebuff

1. Fix the fixture — rewrite the recording rubric. (Confidence: high.) Finding 1 is the session's strongest result: 0/28 states recorded the relation because the rubric asked for "values, names and quotes" and the teacher demonstrated recording structure (imports, constants, function-name lists) and never semantics. Fixing the fixture costs $0 (rewrite the  verbatim  rubric to demand "what each function computes, not just its name and location"), re-probe on the frozen student, and measure. If the relation enters the state, the demand was the bottleneck and this is the cheapest fix. If the suppression effect (finding 7) blocks it anyway, you've ruled out the simplest explanation and the training/prompt fixes are next. Do not start with training or representation changes until the demand is tested — the pattern this session was "interpretations died to measurement," and this one is measurable for $0.
2. I favour the rubric effect, and the cheapest discriminator is the ADD-license control already designed in the arbitration (§4B). (Confidence: medium.) Both operative rubrics push assimilation into existing entries; neither licenses ADD for genuinely new material. The step function (7/10 at 0 entries → 0/10 at 25) is consistent with a categorical instruction: "state present → maintain it, no state → describe freely." The two discrimination controls are well-designed: (a) same probe on untrained floor + untrained 3B (if untrained models keep reading with state present, it's a training artifact), (b) same probe with a rubric that explicitly says "if the view contains content with no home in the state, ADD a new entry." Run (b) first — it's cheaper and more directly tests the mechanism. If recording returns, the fix is a one-sentence prompt change. If both controls are negative, the structural reading survives and that is the first measured support for lever 10.
3. Sound in principle, but the suppression effect kills it on current weights, so it's not testable until finding 7 is resolved. (Confidence: high.) Placeholders create REPLACE targets, which is what the protocol's own mechanics require. But if any non-empty state suppresses recording of new view content, a state full of  TBD  slots still suppresses recording — the model will "maintain" the placeholders rather than fill them. What breaks first if adopted: the state budget gets worse (placeholders add tokens before they save tokens), the model needs to be trained to emit them (0.03% locator-hint rate in training), and until the suppression effect is resolved, they're inert scaffolding. Test on frozen weights only after the ADD-license discrimination control returns positive; otherwise train them explicitly.
4. Yes, hard-cap with LRU eviction by last-touched pass. (Confidence: high.) The budget is stated (4,000 tok), never enforced, and ran to 1.92×. At pass 26 the state was 69% of the model's input — the model is spending its attention budget on its own inventory, not the source. LRU eviction: entries not ADDed/REPLACEd in the longest time are evicted first. Cost: potentially losing information that becomes relevant later. But the current design already loses information (the model can't use its own 7.7K-token state), so the bar is low. Test on frozen weights: re-run a short battery with forced eviction at 4,000 tok and measure RECORD. If recording improves (because the state is smaller and new content isn't crowded out), the budget was load-bearing. If it doesn't change, the suppression effect isn't about size.
5. (a) 3B, repaired data. (Confidence: high.) The GATE-3 scale ladder is the single most decision-relevant number in the project: 3B untrained composes 9/10 on the identical seeded state where 1.5B trained manages 1/10 (p≈5×10⁻⁴). Composition is scale-limited; recording is not. The dissociation — "training bought protocol-following, scale bought composition, no run has had both" — names the empty cell that answers the architecture question. A trained 3B on repaired data (rubric demanding relations, ADD-license for new content, enforced state budget) tests whether protocol-following and composition can coexist. (b) 1.5B is wrong because the compose ceiling (1/10 even clean) caps end-to-end at near-zero regardless of data quality. (c) wastes the training budget on measurement when the measurement is already clear. If forced to pick (d): train 3B on the SAME data as a scale-only arm (repaired data on the next run) to isolate scale from data effects.
6. Yes, legitimate, but not sufficient alone. (Confidence: high.) "Answer from the state alone, source discarded" constrains the information source, not the access method. The  distilled  probe proves retrieval-from-state works: same model, same state, oracle-filtered to relevant entries → composes correctly. The legitimate version: at close, embed the question, retrieve top-k state entries by cosine similarity, compose from those. BM25 is simpler and doesn't require an embedding model. Either way, this is a close-time retrieval scaffold — a legitimate use of the state, not the source. The catch: it doesn't fix recording. If the relation is never in the state (0/28), no retrieval finds it. Test as a companion arm, not a replacement for fixing the recording demand. And it doesn't require retraining — inference-only, $0.
7. Rule: sporadic success counts when the mechanism reproduces under controlled ablation with p < 0.05 against the untrained floor. (Confidence: high.) The  distilled  probe qualifies (remove distractors → compose fires — a reproducible mechanism, not a lucky string). The 1/10 GATE-3 compose at 1.5B does not qualify (n=1, temp 0.7, could be sampling noise from a near-zero rate). The 9/10 at 3B unequivocally qualifies (p≈5×10⁻⁴). Two requirements for the project to adopt: (1) never pool a temp-0 anchor with sampled draws (already a rule), (2) report both the count AND the Fisher exact p-value against the untrained floor, not against zero. A 1/10 that is also 0/10 on the floor is uninformative; a 3/10 where the floor is 0/10 at p<0.05 is signal.
8. Hallucination check. Three issues. (Confidence: medium.)
- The  distilled  probe's 1/10 is treated as dispositive but it's 1 of 2 operand-holding draws. Seed 2 recovered under distillation; seed 5 didn't. The claim "it held the answer and could not find it" is true for exactly one draw. The rhetoric generalizes from n=1. Not wrong, but overheated.
- The 0/28 relation count is retrospective. It was measured during arbitration, not pre-registered. The archived states are the reliable layer (the maintainer correctly notes interpretation was the failure mode, not the archive), but verify: are all 28 states (11 student + 11 floor + 6 3B) actually archived, and does the  rescore  function in  analyze_chain_gate.py  look for relations? It does not —  rescore  tracks the mapping, the adder, and precomposed answers, but has no pattern for the function body or the  +  relation. The 0/28 measurement was done by hand or by a different script. This should be mechanized before it's treated as settled.
- Too tidy. Finding 1 ("the rubric never asked for it") + finding 2 ("the close was never trained as QA") + finding 3 ("scaffolding never trained") + finding 4 ("budget unenforced") + the teacher's modal policy (61.4% lossy summaries) all point the same direction: demand-side defects, not architecture defects. This is suspiciously convenient — every failure has an external explanation. The risk is that fixing all five demand-side defects reveals a sixth, deeper one. The suppression effect (finding 7) is the candidate sixth.
9. Strongest honest claim: "At 1.5B, pass-conditioned LoRA training teaches near-perfect protocol execution (edit-op validity 0.94–1.00, 7.0 applied ops/pass, zero retention loss) over 31 scheduled passes spanning 64K tokens — and the resulting ~7.7K-token integration state is unusable by the model that built it. The compose primitive is scale-limited: an untrained 3B composes 9/10 on the identical task where the trained 1.5B manages 1/10, but scale does not buy recording. Protocol-following and composition have never coexisted in one run." (Confidence: high.) What's missing for it to matter: one cell filled — a trained 3B on repaired data where protocol-following AND composition fire together. Until then, the project has demonstrated a reliable write-only memory and a scale-dependent compose primitive, but not that the architecture's proposed mechanism (integrate-then-compose) actually works end-to-end.
10. Steelman: The pipeline is unsalvageable. The training demand doesn't match the eval demand (analytical brief vs. ANSWER: integer) — a train/eval mismatch that invalidates any ML result on its face. The teacher's modal policy (61.4% lossy summaries, 0.03% locator hints) is actively harmful and the student faithfully copies it. The length gate silently deleted 52% of the only rubric demanding exact values. The fixture requires a relation no prompt ever asked for, shown once at peak state pressure. The state budget is unenforced. The closing rows were excluded from the training-health gate. Four interpretations were formed and withdrawn in one session. Each defect alone warrants a pause; together they describe a pipeline where every layer from data generation through evaluation has a known, consequential defect. A from-scratch rebuild with matched demands, a constructed forward process, enforced budget, and the relation demanded explicitly would be faster and more trustworthy than patching seven interacting failures. Do I buy it? No. The steelman confuses "many defects exist" with "the defects require a rebuild." All seven are demand-side — fix the prompts, not the pipeline. The data exists (5,730 rows), the student exists, the harness exists. The cheapest path is: (1) fix the recording rubric to demand relations, (2) fix the close to match training, (3) add an ADD license, (4) enforce the budget — then re-probe on frozen weights for $0. Only if those probes all fail does a rebuild earn its cost. The steelman replaces a spark plug by machining a new engine block.
11. The diffusion correspondence. (Confidence: medium.)
(a) The constructed forward process is practically useful but the analogy is strained. Diffusion's forward process works because it's stochastic and destroys information in a known, simple way (Gaussian noise), so the reverse process learns a distribution. Compression is deterministic and semantically selective — it decides what matters, so its inverse is a harder learning problem. The constructed forward process (blanking gold states into typed placeholders) dodges this by being manually defined, but creates a circularity: the teacher demonstrating fill-in would need to have already read the source, which is what the model is supposed to learn. This is curriculum learning with a degradation schedule, not diffusion. That said, it's still better than the current pipeline (cloning one teacher trajectory) because it's path-independent and produces supervised pairs without teacher rollout.
(b) Yes, the Phase-A/Phase-B mixture breaks the framing and should be separated. Phase-A (coarse→fine granularity over the whole document) is diffusion-like: each pass refines the resolution of previously-seen content. Phase-B (sequential coverage of different regions) is filtering: each pass processes new observations. These are different information dynamics and calling both "timesteps" obscures the difference. The honest design: Phase-A passes are denoising steps (the correspondence holds); Phase-B passes are filtering steps (it doesn't, and shouldn't be forced). Or restructure so all passes are coarse→fine over the whole document, dropping sequential tiling, which makes the correspondence cleaner but requires the model to hold the whole document structure in attention from pass 1.
(c) Cheapest falsifying measurement: vary only the pass-position label  k/K  on the same tile and rubric, and measure placeholder rate vs. exact-value rate. If  k/K  is functioning as a timestep, early  k  should produce more placeholders and late  k  more exact values. If behavior is identical regardless of  k , the conditioning channel isn't doing diffusion-style work and the correspondence is post-hoc. Costs $0 on frozen weights: 10 draws at k∈{5, 15, 26}, same pass-26 tile, same rubric.
What's genuinely different from prior discrete diffusion: The corruption operator is lossy compression (semantic, not stochastic); the "latent" is an integration workspace, not a noisy version of the output; and observations are external views injected at each step rather than pure denoising from a prior. This is closer to conditional diffusion for data assimilation than masked language modeling. Whether this is insight or metaphor depends on whether the conditioning signal (the view) or the denoising signal (the state trajectory) dominates — if the view dominates, it's a filter; if the state trajectory dominates, it's a diffusion. The current data (monotonic accretion, 725→7,692 tok, no resolution phase) says it's currently a filter with a diffusion aesthetic.

---

kimi-k3-qoder

   1. Fixture first. The 0/10 is overdetermined: unpassable chain, untrained close format, and state-pressure suppression are each independently sufficient. Until the fixture can separate "correct editorial omission" from "capability failure," no training
    or representation change produces an interpretable number. Single highest-information change: rewrite the recording rubric to demand relations ("record what each function computes and where its inputs come from") and give the function body ≥3
   exposures at varying state pressure. Zero training spend, and it makes every subsequent result decidable. Confidence: high.

   2. Favour the rubric effect, structural second. The schema-independent control kills "attachment to own state" but is predicted by both remaining hypotheses: the rubric says "integrate into the entries it belongs to" — with no matching entry, nothing
   is added — and structural saturation predicts the same step. Cheapest discriminator: 2×2 on the pass-26 tile with a fixed 25-entry state, n=10 per cell: {trained 1.5B, untrained 3B} × {current "integrate" rubric, "record any content not already present
    as a NEW entry" rubric}. Recording restored by the record-new rubric → rubric effect. Identical step in the untrained 3B → training-artifact dead. Suppression across all four cells → structural. ~80 draws, inference-only, fits the 1080 Ti. Confidence:
    med.

   3. Sound direction, but it relocates half the failure. REPLACE needs a target and later passes need a declared need; typed slots supply both, and Phase-A views already leak names ("defines helpers: compute_line_surcharge"), so slots can be named before
    values exist — that part genuinely fits. What breaks first: slot prediction — the model must guess which tables/regions matter before seeing values, and wrong guesses bloat the state (the budget problem worsens), while pass 26's single exposure is
   untouched — the fill still happens once, at maximum pressure. Slots fix addressability, not exposure. Adopt only paired with Q1's fixture fix, or it fails identically. Confidence: med.

   4. Yes, hard cap, harness-enforced, evictions logged. A 1.92× overrun means the evaluated regime — state at 69% of input during the one critical pass — was never designed, and finding 7 makes overrun a suspect, not a cosmetic issue. Policy: token cap
   with LRU eviction by last-edit pass, with unresolved placeholders pinned (evicting a slot deletes the need; evicting a resolved value loses a fact — pin the cheaper loss). Costs: (a) a new silent failure mode — recorded-then-evicted is
   indistinguishable from never-recorded in the final answer, so emit eviction events and report "evicted" as a third outcome class in scoring; (b) later-pass behaviour now depends on harness state the model can't observe — you already re-inject the entry
    list each pass, which mitigates this. Confidence: high on the cap, med on the specific policy.

   5. (c) — no training run yet. Three reasons. (i) Finding 5: an untrained 3B already composes 9/10 — the joint training was meant to teach is not the binding constraint. (ii) Findings 6 and §2: RECORD never worked and the data never demanded it, so
   "repaired data" is a hypothesis — and this project's last four hypotheses were all withdrawn on measurement. Don't spend the single run on an unvalidated repair. (iii) Rebuild-and-remeasure is nearly free: untrained 3B f16 fits the 1080 Ti. Decision
   rule: if untrained 3B passes the repaired gate, the architecture claim stands without training; if it fails specifically on RECORD with the relation demanded and multiply-exposed, the run has a validated target and goes to 3B. Confidence: high.

   6. Legitimate, under three conditions. Top-k selection over the state satisfies "answer from the state alone" — it's inference over an allowed artifact. Conditions: declare the retriever and k as architecture and pre-register them before the gate; the
   baseline must be retrieval over a budget-matched flat summary, else the comparison is rigged; oracle-filtered results are ceiling estimates only — the filter sees the answer, which is leakage, so report it as an upper bound, never a result. Note what
   it concedes: the raw state no longer fits the close context, so the claim becomes "bounded store + retrieval," not "bounded state." Confidence: high.

   7. Rule: sub-bar success is noise by definition — that is what the bar is for. Promoting a sporadic success to capability evidence requires (i) replication above the pre-registered bar on fresh draws, never the draws that produced it; (ii) provenance —
    the winning run's log shows the needed facts entered the state via the intended op path, not a leak; (iii) a Clopper–Pearson interval on the new n excluding the floor rate. 1/10 against a 5/10 bar is exactly the species of result that vanishes on
   replication. Adopt: no hypothesis may be formed from a post-hoc success without a fresh pre-registered confirmation run. Confidence: high.

   8. No hard contradiction found; three soft spots. (a) The narrative centres finding 1, but the failure is overdetermined — fixing the chain alone may not move the number. (b) Finding 7's "7/10 relation recorded" is, by its own caveat, 7/10 attention
   with guard-blocked ops — the column overclaims. (c) The floor's 0/10 is uninterpretable as reported: if the floor was scored under the same strict ANSWER: format, it may have failed on format, making student-vs-floor uninformative in both directions.
   Also, 61.4+34.3+4.3 = 100.0 exactly — plausible, but check the rounding. Verified at source: ANSWER: absent from training rows (present only in gate/probe scripts); state_budget_tok is a prompt-rendered warn threshold (teacher_config.yaml:25).
   Confidence: med.

   9. Strongest honest claim: "The failure of a multi-pass integration-state reader decomposes: composition over a clean state is scale-limited (untrained 3B 9/10 vs trained 1.5B 1/10, p≈5×10⁻⁴), recording is not bought by scale (3B 0/5), any non-empty
   state suppresses new recording as a schema-independent step, and trajectory-cloned data never demanded the scored behaviour." That is a real, checked, negative decomposition of this design pattern. What's missing for it to matter: a pass on the
   repaired fixture against a budget-matched baseline — evidence that with the joints fixed, the schedule beats something. Today it is a good autopsy of an artifact nobody else has. Confidence: high.

   10. Steelman: every stage is compromised — rows lack the scored format, the health check excluded the scored stage, the rubric never asks for relations, the budget is unenforced, exposure is once-at-peak, the teacher is 61% lossy, and the data clones a
    single path. "Patching" touches all six: that is a rebuild with worse provenance, inheriting 5,730 rows that encode the old policy and can never serve as comparisons for new arms. With interpretation 0-for-4, continued surgery on an artifact nobody
   fully understands is how projects rot. Do I buy it? For the data pipeline, yes — rebuild around constructed pairs and rubric-first design. Not for the harness: the guard, the seeded-compose battery, and the archived artifacts are reusable
   instrumentation and regression baselines. Rebuild data and fixture; keep the measurement apparatus. Confidence: med.

   11. (a) Mostly post-hoc metaphor with one productive consequence. Diffusion's reverse process is learnable because the forward corruption is known, memoryless, and gradual enough that x_{t−1} is largely recoverable from x_t plus learned priors —
   linguistic redundancy makes masked tokens predictable. Your corruption is deterministic, semantically selective, and deletes precisely the arbitrary facts (23→988) that have zero redundancy: they are unrecoverable from the state, and the information
   arrives from a new view. That is not denoising; it is slot-filling from observation — POMDP belief-state supervision, or curriculum learning. The genuinely useful import is the constructed forward process as a pair generator: typed placeholders give a
   resolution order, targets at every level, path-independence by construction — strictly better than cloning one teacher trajectory, at zero teacher budget. Keep that; drop the claim that the reverse process is diffusion-like. (b) Yes, the mixture breaks
    the framing. Diffusion refines the same object across t; Phase-B changes which object is observable — first exposure, not refinement — giving "timestep" two meanings. Separate them: k/K indexes resolution (placeholder→value); a coverage counter
   indexes region novelty. (c) Cheapest falsifier: blank a gold state to level t on arbitrary-value entries, request the t−1 state with no new view, across t. Recovery ≈0 at all t (predicted) → the timestep carries no denoising semantics and the
   correspondence is vocabulary; monotone degradation in t → something diffusion-like exists. Second cheapest: two blanking orders to the same level — path-dependence kills the required Markov property. Prior art (MDLM, SEDD, LLaDA) noises the generation
   target in token/latent space. What is genuinely different here: the "latent" is an external, symbolic, guard-checked disk state, corruption is compression of an external document, and information enters by scheduled observation rather than reverse
   transitions. Novel as engineering; as theory it is belief tracking, and the diffusion name buys the constructed-pairs trick and nothing else. Confidence: med-high.
   
---

mimo-2.5-pro-freebuff

1. Fix the training demand first. Highest information per unit spend. Finding 1 shows the recording rubric asks for "values, names and quotes" — never relations — and the closing demand asks for "the analytical brief" — never an exact integer. Both are prompt-level fixes with zero new teacher spend if you re-render existing rows. Fixing the fixture (exposing the relation more than once) is necessary but insufficient: if training still never asks for relations, a fairer fixture just produces a cleaner zero. Fixing the representation (lever 10) is the most expensive and premature before you know whether repaired demand works. Confidence: high.
2. I favour the rubric effect, but the step-not-slope pattern is the real clue. The integrate rubric says "integrate this view's content into the entries it belongs to"; the verbatim rubric says "NO_CHANGE if this view adds nothing your state needs." Both instruction sets tell the model to assimilate, not to add genuinely new material. The different-schema control (1/10 vs 0/10, Fisher p≈0.5) rules out schema shape but does not test rubric wording. Cheapest discriminator: same crowding probe with a rubric that explicitly says "ADD new entries for content that has no home in the current state." If recording returns to ~5/10+, the rubric is the lever and it's a one-line fix. If still ~0/10, test untrained floor/3B next. Only if both are negative does the structural reading survive. Confidence: med.
3. Sound in principle; what breaks first is the op cap consuming all budget on slots. If scaffold passes emit 24 typed empty slots (one per ADD), the 24-op cap is spent entirely on scaffolding and zero ops remain for content that pass. Later passes then have REPLACE targets — good — but the scaffold itself carries no information. Mitigation: cap scaffold-slot ADDs at, say, 8 and reserve ops for real content. Second failure: the model may emit malformed placeholders (TBD is not a valid edit op). Third: the empty slots themselves become the suppressing schema from finding 7 — a state of 24  <TBD>  entries is still a non-empty state. You'd need to verify the suppression effect does not fire on placeholder-heavy states. Confidence: med.
4. Yes, hard-cap. The 1.92× overrun is a confirmed harness defect. Policy: least-recently-replaced eviction — entries untouched by REPLACE in the last N passes are evicted first, since the exploration ledger shows the student grinds its own inventory (GROUP 92, GROUP 93…) while ignoring new content. Cost: evicted entries may carry the relation or the formula from early passes. This is the tension: eviction keeps the state small enough for the model to retrieve from, but may discard exactly the entries that matter at close. Measure the eviction-vs-recording tradeoff on a short battery before committing. Confidence: high on the cap, med on the policy.
5. (c) No training — rebuild the fixture and re-measure first. The audit proved the chain was structurally unpassable: the relation was never recorded, the close was never trained as QA, the length gate silently deleted 52% of verbatim rows, and the health gate excluded closing rows entirely. Training a model on repaired data against a fixture you know is unfair is optimizing for the wrong target. Run the two $0 suppression controls from the arbitration, enforce the budget, fix the training demand to match the eval demand, and re-measure the component rates on the existing student before spending a training run. If RECORD improves on frozen weights from demand fixes alone, you learn the training data was the bottleneck. If not, then train — on 3B, because finding 5 says 1.5B is compose-capped anyway. Confidence: high.
6. Legitimate as a harness improvement, but it concedes the architecture's premise. "Answer from the state alone, source discarded" means the state must be self-sufficient. If you need BM25 over the state to find the answer, the state failed its job. Use it as a diagnostic (it already was — the  distilled  probe is oracle-filtered retrieval), not as a production component. If the architecture only works with a retrieval step over its own output, the honest conclusion is that flat text state cannot support implicit retrieval at this scale, which is the lever-10 argument. Confidence: high.
7. Adopt the maintainer's rule: an anomaly counts when the mechanism reproduces under ablation. The  distilled  probe qualifies (same state, same model, distractors removed → 2353; the mechanism is "retrieval from a small state works"). A bare n=1 compose (e.g., cross-family seed 9) does not — it could be temperature-driven sampling. Concrete rule: a sporadic success becomes evidence of latent capability when (a) it replicates at n≥5 with a rate significantly above 0 by binomial test, and (b) the mechanism is identified (which component improved and why). Below that threshold, report it as a data point, not a finding. Confidence: high.
8. One tension, not a fabrication. The document says "training moved every component" and separately that "the improvement and the recording failure may be the same mechanism" (larger state = stronger suppressing schema). These are presented as compatible but they point in opposite directions: if training's improvements are causally linked to the failure, calling them "progress" is misleading. The numbers are internally consistent; the framing pulls in two directions. Separately: the  exactask  probe scored 0/10 but seed 2 moved from 86 (decoy) to 1365 (real operand) — this is treated as a flat negative but it's a qualitative shift in where the model retrieves. Not wrong, but under-discussed. Confidence: high.
9. Strongest honest claim: "A 1.5B model can be trained to execute a structured multi-pass reading protocol (edit-op validity ~0.97, 7.0 ops/pass) and build a 7,686-token integration state across 31 passes, with perfect retention of recorded content — but cannot compose two operands from that state, and the training pipeline never asked it to." What's missing for it to matter: one end-to-end success on a non-trivial compose task under the protocol, at any scale. Without it, the positive finding (protocol compliance) is an engineering result demonstrating a reliable write-only memory. The M-NL nuance experiment is the right next test of whether the architecture does something a simpler baseline cannot. Confidence: high.
10. Steelman: The fixture required a formula no prompt ever requested, exposed it once in 31 passes at peak state pressure, and was scored on a format (ANSWER: integer) that appears zero times in training. The training pipeline deleted 52% of the one rubric demanding exact values, excluded the closing stage from the health gate, and produced a teacher whose modal recording policy was lossy summary (61.4%). The suppression effect means training made the state more uniform and harder to read from, not less. Every element the chain required was something the pipeline never asked for. Patching is rearranging deck chairs: fix the demand and you still have a 1.5B model with a 1/10 compose ceiling on a clean state; fix the scale and you still have a flat text state that suppresses retrieval at ~25 entries. Do I buy it? Mostly. The training pipeline defects are real and patchable — demand mismatch, length gate, health gate exclusion are all fixable without rebuilding. But the fixture is genuinely unsalvageable as a sole verdict instrument: it cannot separate capability failure from correct editorial omission (the P7 relevance fork is still unresolved). Rebuild the fixture; patch the pipeline. Confidence: med-high.
11. (a) The constructed forward process is the strongest idea here, but compression's selectivity breaks the clean analogy. Blanking gold states into typed placeholders is path-independent, cheap, and doesn't require teacher rollout — this is genuinely useful regardless of whether the diffusion label fits. But diffusion's forward process is stochastic and uniform: any pixel can be masked. Compression is deterministic and semantic: it decides "this helper name matters, this boilerplate doesn't." That means the noise levels are not interchangeable — you cannot randomly blank entries and call it a corruption process, because the entries have unequal importance. You need importance-weighted blanking, which reintroduces the teacher's judgment into the forward process. (b) The Phase-A/Phase-B mixture does strain the framing. Phase-A is coarse-to-fine over the same content (diffusion-like: granularity refinement). Phase-B is sequential coverage of different regions (filtering-like: new information arriving). These are different operations and should be decomposed. The diffusion label fits Phase-A; Phase-B is closer to Bayesian filtering (the project's own  DISCO-NATIVE-ARCHITECTURE.md  acknowledges this). (c) Cheapest falsification: measure state specificity (fraction of entries containing exact values vs placeholders/scaffolding) across passes on the archived trajectories. If it's monotonically increasing (accretion), the diffusion framing is wrong — there is no denoising. If it's non-monotone (placeholders rise then resolve), the diffusion framing has empirical support. This costs $0 and can be done today on archived data. Prior art: Masked diffusion LLMs (LLaDA, MDLM, HDLM) corrupt by masking tokens in the output and learn to unmask; they are same-length generation. What's genuinely different here is that the "corrupted object" is a structured external state, not the model's own output — the model reads a source and writes to a separate state, which no masked diffusion LM does. Whether that difference is deep or cosmetic depends on whether the constructed forward process actually produces learnable reverse trajectories, which is untested. Confidence: med.

sonnet-4.5-kiro

  1. Fix the representation, then the fixture. High confidence. The rubric never requests relations ("values, names and quotes"), the recording demonstrations show lossy naming 61.4% of the time, and the closing demand strips specifics from the brief. You
  cannot supervise a behavior you never demonstrate. The single highest-information move: take 5–10 manually curated gold states that do contain the relation type you need, append the pass-26 tile, and measure if the trained 1.5B can ADD the relation when
  the state shows examples of relation-recording. Cost: 2 hours, zero GPU. If it succeeds, the rubric needs relation examples and the fixture needs multi-exposure. If it fails, the 1.5B cannot learn the recording pattern even with demonstration.
  2. Rubric effect. Medium confidence. The instruction "integrate this view's content into the entries it belongs to" makes REPLACE the natural operation when entries exist, and REPLACE requires you to recognize an existing entry rather than record new
  structure. The step function (empty→any non-empty) plus schema-invariance suggests the rubric is selecting a different mode, not that accumulation is structurally blocking. Cheapest discriminator: same pass-26 tile, 25-entry state, but change the rubric
  to "Record any new functions, tables, or mappings this view introduces, even if similar entries exist. Prefer ADD for new structure." Run n=10. If recording recovers, it's rubric. If it stays suppressed, it's accumulation or training artifact. Cost: <1
  hour inference.
  3. It's necessary but insufficient. Medium-high confidence. Typed slots fix the REPLACE-without-target problem and create a denoising training signal. What breaks: (a) slot typing has to be semantically meaningful or the model has no basis to match
  "this mapping" to <table> vs <constant> vs <function>—you need a small controlled vocabulary or the matching problem is as hard as freeform recording; (b) early-pass compression may not show enough to type the slot correctly (a table mentioned by name
  only cannot be typed as <mapping>); (c) if K is large, intermediate passes see a forest of placeholders and no selection pressure—what to refine first? You will hit "placeholder proliferation" where the model creates slots it never fills. Test: manually
  construct a 3-level chain (absent → typed slot → filled) for the table, adder, and function. If 3B can complete it, the scaffold is sound. If it completes table and adder but not function, typing is insufficient for relations.
  4. Yes, hard-cap with LRU eviction scored by recency-of-edit. High confidence. Running to 1.92× budget means (a) the model never learned budget discipline, (b) the state crowded out the source tile by pass 26, and (c) you have no deployment story if the
  state is unbounded. Eviction policy: when an ADD would exceed the cap, remove the entry with the oldest last-modified pass number (not the oldest creation pass—early scaffolds should survive if they stay relevant and get REPLACEd). Cost: the model may
  evict something it needs later, but that failure mode is visible and measurable, whereas unbounded states fail silently by context dilution. Implementation cost is trivial. Alternatives like embedding-based relevance scoring are expensive and add a
  retrieval model you'd need to train.
  5. (c) Rebuild the fixture and re-measure first. High confidence. Findings 1–4 are all fixture and data pathologies, not capability failures. You do not know what the current data can teach because the test never evaluated what the training
  demonstrated. Spending a training run on unvalidated data is buying a lottery ticket. Rebuild: (1) multi-expose the relation (passes 12, 19, 26), (2) add closing rows to the health check, (3) revise the rubric to request relations and show recording
  examples, (4) manually construct 20 gold multi-pass trajectories with placeholders on the same synthetic fixture, (5) run the untrained 3B (which already composes 9/10) through the repaired chain. If it passes, you have a working scaffold and the 3B is
  your deployment target; training the 1.5B becomes optional. If it fails, you have localized the failure before spending GPU budget. Cost: 2–3 days of manual curation.
  6. Legitimate, necessary, and you should do it. High confidence. The constraint is "answer from the state alone"—retrieval over the state satisfies that. The source is discarded; the state is not. If the state is 7,692 tokens, the model cannot attend
  uniformly—retrieval is selecting what to attend to, which is what attention does anyway, but explicitly. The alternative is to make the state small enough to attend to fully (see Q4), but that requires eviction, which is implicit retrieval. The
  oracle-filtered success is not a concession; it is a proof that the state contains the answer and the failure is attention allocation. Deployment cost: BM25 is ~free, embeddings require a small encoder but that is a sunk cost in any real system. Do it.
  7. Adopt n≥10 and Fisher p≤0.05 as the discriminator. High confidence. Sporadic success at n=1 or 2 is noise. At n=10, 1/10 vs 9/10 is signal (p≈0.003). The 3B untrained result (9/10 compose) is not latent capability—it is direct capability, and the
  surprise is that it exists untrained. Latent capability would be 2/10 trained vs 0/10 untrained at the same scale. Rule: if an intervention moves the rate by ≥4/10 at n=10, it is real. If it moves by 1/10, run n=20. If you cannot afford n=20, you cannot
  afford to trust the result.
  8. Two things. Medium confidence. (a) "3B untrained (f16) 9/10" vs "3B untrained (Q4) 7/10"—if quantization only hurts and 7/10 is still far above the trained 1.5B, why is Q4 viable for 7B but degrading for 3B? Is the 3B result near a threshold where
  quantization noise matters? Replication at n=20 would clarify. (b) The seeded-compose test uses a "frozen 8-entry seeded state." What schema? If the seed demonstrates relation-recording and the test is "add one more," that is not composing—it is
  imitating. If the seed is unrelated and the model synthesizes the relation, that is genuine. The text does not specify.
  9. Honest claim: "Untrained 3B models can perform 3-hop composition over a structured text state at 9/10 accuracy when the state is small (8 entries) and the query is explicit. Scaling to 64k-token documents and implicit queries fails due to state size
  (1.92× budget, 69% of context), single-exposure to critical content, and training data that never demonstrates the target behavior." Missing to matter: a working fixture on the 3B untrained that succeeds end-to-end at n≥10, proving the architecture can
  scale before you invest in training. If the 3B fails even with repaired exposure and budget, the architecture is unviable at this scale.
  10. Steelman: The teacher policy is unrecoverable. The 277-document rollout never recorded relations (0/28), demonstrated lossy summarization 61.4% of the time, closed with a prose brief that strips specifics, and used a prompt vocabulary (analytical
  brief) that never appears in the verdict (ANSWER: <integer>). You could patch each failure, but every patch fights the teacher's learned prior. The scaffold rubric says "don't chase specifics" but REPLACE-bias requires specifics to exist; that is a
  contradiction at the protocol level, not a tuning error. The closing instruction was excluded from the health check, so the pipeline never gated the only stage that matters. Rebuilding means: write 20 gold trajectories by hand on the same fixture, use
  them as few-shot examples for a better teacher (or skip the teacher—20 trajectories × 31 passes × 10 augmented variants = 6,200 rows, comparable to the current 5,730), and this time include the closing row in the health check. Do I buy it? Yes,
  medium-high confidence. Patching would require: new rubric, new closing instruction, re-rolling 277 documents, re-checking 5,730 rows, and hoping the teacher generalizes to relation-recording despite never having been asked. Manual curation of 20
  trajectories is probably fewer hours and eliminates teacher drift. The cost is you lose coverage diversity, but the current diversity did not help.
  11. The correspondence is real but underspecified. (a) The constructed forward process works in principle but determinism is not the problem—semantic selectivity is. Medium confidence. Diffusion corruption is non-semantic (masking does not care what the
  token means), so the reverse process learns a general denoising function. Blanking 23: 988 → 23: <cents> is semantically typed—you have decided <cents> is the right slot type, which requires understanding the role of 988. That decision is the hard part.
  If you blank incorrectly (23: <region_id>), the corruption is adversarial and the reverse process learns gibberish. The forward process is only "constructed" if the typing is algorithmic. Diffusion's forward process is parameter-free; yours requires a
  semantic parser. That said, if you can type the slots (e.g., with an LLM or rules), you do get path-independent training data, which is a real win over trajectory distillation. (b) The Phase-A/Phase-B mixture does not break the framing—it breaks the
  monotonicity of k. Medium-high confidence. In diffusion, timestep t=0 is pure noise, t=T is clean data, and t is monotonic in signal. If Phase-A is coarse-everywhere and Phase-B is fine-sequentially, then pass 15 might see coarse structure in region X
  and fine detail in region Y. The noise level is spatially heterogeneous, so k/K is not a sufficient statistic. You would need per-region or per-entry conditioning (k_i / K for entry i), which is expensive. Separate schedules would clarify the framing
  but require two models or two conditioning schemes. (c) Cheapest falsification: train a tiny model (100M params) to denoise synthetic manually-blanked states, zero-shot it on a different fixture, and measure if it generalizes. If denoising is learnable
  like diffusion's reverse process, the model should fill typed slots it has never seen. Cost: <4 hours on the A5000, synthetic data only. Genuinely different from masked diffusion LLMs: prior work masks tokens randomly and learns to predict them from
  context. You are masking semantically (values but not keys, late-pass content but not early-pass structure) and conditioning on document position (k/K), which is closer to hierarchical or cascaded generation than diffusion. The analogy is looser than
  the framing implies.
  
---

qwen-3.8-qoder

 ▪ 1. Fix the fixture. The chain was unpassable as scored — 0/28 states recorded the relation because no rubric ever asked for it. This is not a model failure; it is a measurement failure. The single highest-information change: expose the function body in
    ≥3 passes (not just pass 26) and add "record what functions do" to the verbatim rubric. This costs $0 (compressor config + rubric text) and immediately separates "the model cannot record relations" from "the model was never asked to." Training fixes
   are premature until you know whether the demand produces the behaviour. Confidence: high.

   2. Favour the rubric effect. The evidence: at 0 entries the model describes the view freely (7/10 relation), but with any state present it switches to "integrate into existing entries" mode and grinds its own inventory. The rubric says "integrate this
   view's content into the entries it belongs to" — with no entry for a function's behaviour, the model has no legal move and defaults to REPLACE into the nearest GROUP entry. Cheapest discriminator ($0): same probe, same 25-entry state, rubric changed to
    "ADD a new entry for any content with no existing home." If recording returns → rubric. If not, run the same probe on untrained 3B → if 3B records, it is a training artifact. Only if both fail is it structural. Confidence: med (the caveat about
   malformed ops at 0 entries means the 7/10 baseline is attention, not recording — the step may be an artifact of the guard, not the rubric).

   3. Sound in principle, and the protocol's own mechanics require it — REPLACE needs a target, and no target exists without early ADDs of typed slots. What breaks first: the teacher data. You have 5,730 rows with 1.8% placeholders. You cannot re-render
   this for free (changing the prompt invalidates completions), so you need new teacher generation. Second failure mode: slot proliferation. Without a budget on placeholder count, early passes will emit hundreds of "TBD" slots and the state bloats before
   any resolution begins — the monotonic accretion problem returns in a new form. You need a typed-slot grammar with a cap. Confidence: med.

   4. Yes, hard-cap it. An advisory budget that runs to 1.92× is not a budget. Eviction policy: LRU by last-touched pass, with a protected set (entries referenced by the current need_class). Cost: you will evict entries the closing question needs. But the
    current alternative — 7,692 tokens of state consuming 69% of the model's input at the critical pass — is strictly worse, because it suppresses new recording (finding 7). A 4,000-tok hard cap with forced eviction is the cheaper failure mode to
   diagnose. Confidence: high.

   5. (c) No training. Rebuild the fixture and re-measure first. Justification: you do not yet know whether the 0/10 is a capability failure or a demand failure. The audit says demand failure (0/28 states recorded the relation; no rubric asked for it).
   Spending an A5000 run on repaired data for a fixture that may still be unpassable is the expensive way to discover what a $0 rubric probe can tell you. Run the fixture fixes (multi-exposure, relation-demanding rubric, hard budget cap) on frozen weights
    first. If the 3B untrained records the relation under the new rubric, then you know training has something to teach, and you spend the run on 3B repaired data (option a). Confidence: high.

   6. Legitimate, with a caveat. "Answer from the state alone, source discarded" constrains the information source, not the retrieval mechanism. BM25 over the state is still answering from the state. The oracle-filtered version is forcing (you acknowledge
    this), but a non-oracle retriever is a legitimate harness component — it is the closing-step analogue of the compressor. The danger: if retrieval over a flat-text state is the fix, you have built RAG over your own notes, and the architectural claim
   (the schedule produces a state that supports composition) is weakened to "the schedule produces a state that supports retrieval." That is a narrower claim. Accept it if that is what the data says. Confidence: high on legitimacy, med on whether it
   preserves the research contribution.

   7. Rule: a sporadic success is evidence of latent capability if and only if the mechanism reproduces under controlled ablation. The distilled probe qualifies (same state, same model, distractors removed → 2353; the mechanism is "retrieval succeeds when
    the search space is 2 entries"). A bare n=1 cross-family compose does not qualify (no ablation isolates why that seed fired). Adopt: "an anomaly counts when a named intervention flips it from 0 to ≥1 reproducibly (n≥3); a string appearing once is
   noise." Confidence: high.

   8. Two issues. (a) The suppression table (finding 7) reports 7/10 at empty state, but the caveat says those ops are malformed REPLACEs the guard would block. So the "step" is partly an artifact: at 0 entries the model attends to the view but cannot
   record; at 25 entries it records into existing slots but ignores new content. These are different failure modes presented as one continuous effect. The step is real but the interpretation "any non-empty state suppresses recording" overstates — it
   suppresses out-of-schema recording. (b) The claim "compression replacing masking as the corruption operator" (finding 11 framing) is stated as if compression is analogous to noise addition, but compression is deterministic and semantically selective —
   it chooses what to keep. Masking destroys uniformly; compression destroys by judgment. This is flagged in the handoff but not in the prompt's framing section, creating an internal tension. Confidence: high on (a), med on (b) being a genuine error vs. a
    known strain.

   9. Strongest honest claim: "A pass-conditioned schedule with a bounded text state can deliver every relevant fact to the model's attention (coverage works), and a trained 1.5B follows the edit protocol 3× better than its base — but the conjunction of
   record, retain, and compose has never fired reliably at any scale tested, and the fixture as scored was unpassable." What is missing for it to matter: a single end-to-end demonstration, on a fair fixture, where the schedule produces a correct answer
   that a simpler baseline (dense read, RAG) cannot. Without that, there is no result — only infrastructure. Confidence: high.

   10. Steelman: the fixture demands a formula no prompt requests, shows it once in 31 passes at peak state pressure, and scores on a format never trained. The teacher data is 61% lossy summaries with 0.03% locator hints. The training-health gate excluded
    the only stage that matters. The state budget is fiction. Every "fix" (placeholders, relation rubric, budget enforcement, multi-exposure) is a patch on a pipeline whose teacher policy, rubric design, and scoring were all misaligned from the start.
   Patching preserves sunk cost but inherits the pipeline's assumptions — including the assumption that a single teacher trajectory is the right training signal. A rebuild from the constructed-forward-process idea (blank gold states into typed
   placeholders, supervised pairs at every level, no teacher rollout) would be cleaner, cheaper, and path-independent. Do I buy it? Partially. The data pipeline is unsalvageable as-is — you need new teacher generation regardless. But the harness
   (compressor, edit-op grammar, guard, schedule) is sound and tested. Rebuild the data; keep the harness. A full from-scratch rebuild discards working infrastructure to avoid confronting a data problem. Confidence: med.

   11. (a) The constructed forward process is the most concrete and testable idea here, but the analogy is strained in a specific way: diffusion's forward process is stochastic and information-destroying in a known, uniform way, which is exactly why the
   reverse is learnable. Your "blanking" is deterministic and semantically selective — you choose which values to blank and in what order. The reverse process (fill placeholders from a view) is learnable, but not because of any diffusion theorem — because
    it is a supervised seq2seq task with a curriculum. Call it what it is: curriculum learning with typed slots. The diffusion framing adds no mathematical guarantee. (b) The Phase-A/Phase-B mixture is a real problem for the framing. Phase-A (granularity
   refinement over the whole document) is diffusion-like: same content, increasing resolution. Phase-B (sequential coverage of different regions) is filtering: new content arrives. These are different operations. A diffusion model does not receive new
   information at later timesteps. Separating them — or at minimum labelling each pass as refinement vs. coverage — is necessary before the framing makes predictions. (c) Cheapest falsification: measure whether state specificity is non-monotone on
   archived runs. The diffusion framing predicts placeholders rise then resolve (inverted-U). If archived states show monotonic accretion with no resolution phase — and the handoff says they do (725 → 7,692, flat accretion) — the forward process was never
    operating, and the correspondence is post-hoc. Prior art: masked diffusion LLMs (MDLM, Sahoo et al. 2024; SEDD, Lou et al. 2024) use uniform masking as corruption and learn to unmask. The genuine difference here is that your "noise" is compression
   (semantic, deterministic, non-uniform) and your "latent" is on disk in natural language, not in a continuous embedding. That difference makes the diffusion math inapplicable; the intuition (start unresolved, end resolved) is sound but does not need the
    diffusion name. Confidence: high on (a) and (c), med on (b).
	
---

gemini-3.1-pro-studio

1. Confidence: High
Fix the fixture/rubric, and modify the generation. You are failing a test on extracting arbitrary mathematical relations because the recording rubric explicitly demands "exact values, names and quotes" but ignores formulas, and your teacher policy is 61.4% lossy summary. Furthermore, your final evaluation metric tests an out-of-distribution prompt (ANSWER:) rather than the trained target (the brief). The single change yielding the highest information per dollar is deploying a modified, explicit relation-extraction rubric on a purely synthetic state-builder, and testing it zero-shot on the 3B model. Modifying the training pipeline right now just optimizes garbage data.
2. Confidence: High
I favour the rubric effect intersecting with state mechanics. The REPLACE-bias strictly commands the model to integrate data into existing matching entries. When confronted with an empty state, the model bypasses structural search, panics, and dumps the required attention span (as malformed REPLACEs). When even 25 unrelated entries exist, the model attempts strict compliance, fails to find a valid corresponding target for the math function, and quietly discards the novel content to respect the replacement constraints.
Cheapest falsifying experiment: run the identical 26th pass using a state containing exactly one entry explicitly waiting for the target (e.g., S1: compute_line_surcharge <formula slot>). If the model correctly executes, it’s a target-matching artifact.
3. Confidence: High
It just moves the failure point and fundamentally breaks the architecture. Relying on early, low-resolution passes to reliably generate highly-typed semantic empty slots requires impossible foresight. The early pass will create structurally naive slots (e.g., <price: float>). When later high-resolution passes discover complex equations or split references, the observed reality will completely conflict with the early slot ontology. The system will predictably break via refusal to REPLACE (guard rails kicking in on type-mismatches) or rampant hallucination to force-fit correct facts into broken schemas.
4. Confidence: Medium
No. Do not deploy forced algorithmic eviction (FIFO, truncation, or LRU). Imposing arbitrary algorithmic limits destroys the architecture's founding premise of preserving boundless document nuance. The unchecked 1.92× bloat isn't an infrastructure bug; it is an objective-function failure—the model was never penalised for infinite appending nor incentivized to self-compress. The eviction policy must be natively handled by the model executing REMOVE and REPLACE commands based on an explicitly enforced token budget constraint injected into the prompt/rubric.
5. Confidence: High
(c) No training — rebuild the fixture and re-measure first.
The entire baseline is severely contaminated. You evaluated using a string missing from the entire dataset, you gated out the specific final rows your model actually trained on, and your teacher was statistically optimized to destroy the information you seek. Attempting to fine-tune either model (1.5B or 3B) using "repaired data" without definitively fixing the evaluators (scoring the briefs properly, correcting the REPLACE constraints) ensures you will merely perfectly overfit to methodological noise. Rebuild the pipeline deterministically and verify with untrained 3B.
6. Confidence: High
Legitimate under your constraints, but an admission of severe architectural failure. You constrained the rule to "answer from the final state alone"; querying a state index technically honours that string. However, if the generated integration state becomes so fragmented, bloated, and illegible that an LLM cannot synthesise across it natively without intermediate embedding retrieval, then the iterative state has catastrophically failed at structural "integration." You've inadvertently built a highly expensive, multi-pass RAG index generator.
7. Confidence: Medium
Given an iterative pipeline mapping states sequentially via discrete text manipulations across K=31 cycles, anything that requires probabilistic temperature sampling is functional noise, not useful latent capability. Low-probability stochastic successes will exponentially decay to zero chance of completion across extended sequence intervals.
Rule to adopt: Evaluate pass/fail exclusively on Temperature = 0 (greedy decoding). If a capability requires sampling noise to occasionally materialize on the correct structural rail, it is useless for state-machine stability and should be classed as a failure.
8. Confidence: High
Yes. Finding 7 contains an outright contradiction in the reporting. The data table asserts "relation recorded 7/10" for the empty state. Immediately below, the text confesses the ops were "malformed REPLACEs the guard would block, so 7/10 measures attention, not successful recording." If the environment guard rejects the operations, the state mathematically was not mutated, and the relation was absolutely not recorded in any persistent artifact. Conflating model stream emission with committed disk state success artificially props up your tables. Furthermore, excluding your final evaluator gates entirely during training-health checks but relying on it for your project pivot is a crippling lapse in basic methodology.
9. Confidence: High
Strongest honest claim: "This inference protocol aggressively compresses long context arrays into persistent unstructured summary inventories by delegating attention to multi-pass localized chunking."
What is fundamentally missing: Demonstrated execution of the COMPOSE joint. If the architecture cannot systematically synthesize multiple fragmented observations across different views into novel state deductions, then it isn't an "integration state." It is just iterative semantic text copying.
10. Confidence: High
Steelman: You are trying to distill rigorous, mathematically bound integration capabilities using imitation learning from a commercial black-box (Haiku) acting as a stochastic, unstructured 61% lossy summarizer. This is computationally suicidal. Generating data forward by aggregating probabilistic dumps guarantees monotonic text OOM bloat because it inherently lacks structural decay mapping. You absolutely must rebuild using the constructed process—beginning with a perfect, structured total-document truth map, and aggressively blanking entries backward into semantic gaps to guarantee lossless path-independent alignment points.
Do I buy it? Yes. Relying on imitation-cloning for a deterministic structured execution model ensures compounding variance collapse. Start from the gold state and carve it backward to teach gap-resolution.
11. Confidence: High
(a) The constructed backward degradation functions competently for supervised fill-in-the-blank instruction, but completely violates true diffusion dynamics. In diffusion, masking/noise functions locally and without structural bias, permitting continuous reversible approximations. Text compression relies heavily on structural semantic typing; a single incorrect syntax resolution causes global logic failure on text branches, completely shattering the robust, path-independent reversibility seen in diffusion networks.
(b) Yes, this catastrophically breaks the framework. Granularity refinement corresponds to diffusion's structural step recovery. Sequential coverage dictates spatial scope injection. Compounding entirely different physical attributes into a single scalar timeline (k/K) mathematically destroys state continuity. You are attempting to resolve latent noise whilst dynamically feeding previously unindexed documents. Decouple them immediately.
(c) Cheapest falsification: Redact 20% of random linguistic substrings directly from an accurate pass-25 state—removing no explicit entity relations, purely corrupting localized characters/syntax—and force the network to denoise it using a corresponding verbatim chunk. If the network crashes and cannot reconstruct because semantic typing is gone, it is executing strict instruction-matching, completely lacking the spatial resilience of genuine continuous discrete/text-diffusion analogs (such as Masked language models or Embedding diffusions, which recover structural representations probabilistically).

---

minimax-m3-freebuff

1. Fix the rubric. Confidence: medium-high. The audit identified two joint failures in the chain gate: (a) the relation was never recorded — a data defect, the recording rubric (verified in  pca/teacher_gen.py:RUBRICS ) literally says "exact values, names and quotes," never behaviour or relations; (b) even when the relation is seeded into state, the 1.5B has a compose ceiling (GATE-3 1/10). Of the three named options, patch the rubric dominates on information-per-dollar: re-render the 5,730 existing teacher rows under (i) a relation-aware recording rubric, (ii) explicit placeholder scaffolding at scaffold passes, (iii) data-before-instructions prompt order at zero teacher-API cost. Re-probe on frozen weights first; spend the training run only if the floor probe moves RECORD. Don't fix the fixture — it's backfill-defended by construction and doing its job. Don't fix the representation before the $0 probe; latent architecture is a multi-week build that won't salvage 1.5B at any rate.
2. Floor-control probe. Confidence: high. Run the same crowding probe (pass 26, state sizes 0/25/50/100/155) on the untrained 1.5B floor. If the floor mirrors the student (0/10 at every non-empty size), the suppression is structural. If the floor recovers (≥5/10 at empty-state, partial recovery elsewhere), it's a training artifact. One $0 experiment discriminates two of three explanations, tens of minutes of compute. The rubric explanation is then tested by adding "ADD permitted for content with no home in state" to the prompt and re-running; recording returning confirms the rubric.
3. Sound direction; breaks first at fill-via-REPLACE. Confidence: medium. Placeholder scaffolding is right; without it, the contract degenerates to ADD-only and the 24-op cap (verified in  teacher_config.yaml:op_cap: 24 ) closes in mid-table. What breaks first: REPLACE grammar requires verbatim quote-matching; a  <placeholder>  cannot match live operand text. Without a new reconcile primitive, late passes revert to ADDing double-counted entries and the cap hits before the table is filled. Bigger risk: stitching this into the grammar is itself a multi-lever change — rubric re-render + training, not a single fix. Declare it as two levers if you adopt it.
4. Hard-cap, yes. Token-counted, with stage-scaled op_cap. Confidence: medium. Verified:  state_budget_tok: 4000  is rendered into the prompt ( teacher_gen.py:_system_prompt ) but never enforced. Enforce it with harness-boundary truncation; ~1,200-1,800 cap slung across passes is right. Eviction: score each entry by  (recency × role-weight) , default 1.0, demote scaffold on later passes; drop lowest until rebalanced. Cost: 4-byte-per-entry metadata + O(n) eviction per pass. Simpler intermediate: stage-scaled op_cap (24 verbatim / 16 integrate / 8 scaffold) — cuts accumulator growth at the source. Run the op-cap scaling first on frozen weights as a $0 probe; only after that, the eviction policy.
5. (b) 1.5B with re-rendered data, conditional on the $0 floor probe. Confidence: medium-high. Don't pick (a) — 3B same data leaves the data gap; cross-family evidence (Llama-70B 1/10, Mistral-24B 1/10 on both-operands-held) says compose over text-state hits a ceiling regardless of scale. Don't pick (c) — the fixture is working; the failure is data+rubric. Don't pick (d) — M-NL or native architecture dissolves a different joint. Pick b with strict sequencing: $0 floor probe first, then A5000 run only if it moves RECORD on the floor. The repaired run targets the existing ~5K rows (re-rendered), no corpus re-generation.
6. Legitimate under "answer from state alone," with caveats. Confidence: medium. Constraint is no source-reading; a BM25/embed step over the state reads state alone. But this decouples retrieval from the model — a load-bearing architecture change toward MemGPT territory — so it needs a fresh pre-reg. Caveat I'm staking a flag on: the audit's "distilled-2353 at 1.5B" is partly unverified.  probe_closing.py:distill()  keeps entries containing 988 OR 1365; whether those entries co-carry the formula reference ( base + RECONCILIATION_ADDER_CENTS ) depends on whether the function body co-occurs with the constant definition in any single entry — and the gauntlet reviewer flagged this. Unsure until distilled probe transcripts are hand-read; the 2353 may be traceable to model priors on the function name, not to in-state formula.
7. Signal vs sampling noise. Confidence: high. Rule: anomaly is signal when the mechanism reproduces under ablation on the same weights (same model, varying only the dimension the anomaly purportedly depends on); anomaly is noise when it's a single draw at one seed/temp with no ablation. Apply: distilled-2353 = signal (varies operand states); cross-family n=1 composes = noise; 8/10 RETAIN = signal (paraphrase-robust matcher is locked); bare scaffold-only innovations = noise without cross-pass ablation. Reporting rule: every anomaly tag lists draw count + ablation or gets a NE flag.
8. Inconsistencies and unverified premises. Confidence: medium.
- (i) The relation-flag is interpretive, not measured.  analyze_chain_gate.rescore  tracks  queried_mapping ,  adder_value_present , and a  tier1_summary_only  range flag — no formula-text-in-state flag. Finding 1 follows logically (the answer requires the formula), but wasn't a direct measurement. Cross-check final states for any line containing both operands in a  +  or assignment context.
- (ii) Distilled-1.5B-2353 premise is unverified; same caveat as Q6.
- (iii) "8/11 states name the surcharge table; 0/11 briefs mention it" under  traindemand  — value cross-checks against the recorded probe transcripts; unsure whether the exact 11/11 denominator recomputes under a fresh scrub.
- (iv) Qwen2.5 14B Q4 is listed as present in the HANDOFF env inventory, but the gauntlet scale table stops at 7B Q4. Gap unexplained; either mis-stated or the 14B rung was held for a reason not in the record.
- (v) No internal mathematical contradiction;  988 + 1365 = 2353  checks.
9. Strongest honest claim. Confidence: medium. "On a 1.5B student + 5,730 Haiku-rendered rows, pass-conditioned LoRA trains the protocol (validity 0.94-1.00, RETAIN 0/lost, op-type agreement 0.76) but the chain is independently blocked by a joined rubric-data defect (relation never recorded) and a 1.5B compose ceiling that 3B clears on a seeded state." Two findings, not one. Missing for it to matter to anyone outside the maintainer-plus-arbitration circle: a repaired-rubric instantiation that re-establishes RECORD on the same fixture while keeping GATE-3 clean. Without that, the claim is "we localised the joints," not "we fixed them"; and a single sentence, "the chain rebroke after rubric repair," would be the death of the project.
10. Steelman the kill — and do I buy it? Confidence: high. Steelman: the rubric never asked for relations; the verifier's success criterion required a relation; the close demand was OOD; the corpus was rendered under the broken rubric. The failure was predictable from the rubric, not from the architecture. The fix is to rebuild the data + rubric + verifier stack as one unit under a fresh pre-reg — not to scrap the architecture. The fixture, the state grammar, the guard, the GATE-3 design, the post-fix edit-op parser, and the cross-model probe harness are intact. I buy it for "don't burn another 1.5B run on this corpus." I do not buy it for "throw out the architecture" — the cross-family existence proof (Qwen-72B s2, Llama-70B s9, Mistral-24B s42) and 7B Q4's clean-state 10/10 show the chain fires when inputs are seeded.
11. The diffusion correspondence. Confidence: medium.
- (a) The constructed forward process does not preserve diffusion's learnability. Compression is deterministic and content-selective; it obliterates detail the compressor deems non-load-bearing. The (clean, noised) pairs are correlated through semantic preservation only — no stochastic noise, no analytic posterior, no symmetric noise/denoise channel. A learned reverse mapping behaves as content-copying, not denoising. Closest prior art — masked/structured-diffusion LLMs (DiffuLLM, MDLM, SSD/LM, Plaid) — operate on a known stochastic mask, not content selection. Genuinely different here: the corruption operator is itself a model. The correspondence is metaphor.
- (b) Phase-A ⊕ Phase-B mixing breaks the framing on Phase-B. Phase-A is diffusion-like (same document, multiple resolutions); Phase-B is filtering-like (different regions, mostly verbatim), matching the Bayesian-filter spine in  DISCO-NATIVE-ARCHITECTURE.md . Treat them as separate sub-objectives — denoising loss for Phase-A, coverage/retention loss for Phase-B.
- (c) Cheapest falsifier: train on constructed pairs at Qwen 3B (f16 fits the 1080 Ti, ~orders of magnitude cheaper than a 1.5B retrain cycle), then on the same chain battery add a deliberately-noised final state before close (drop k% of operand entries, replace with placeholders, varying k). Diffusion predicts monotone improvement with denoising; stepwise collapse means metaphor.

---

glm-5.2-opencode

1. (high) Fix the representation. Findings 1–3 jointly say the chain never had a target to write the relation into, never had a prompt that asked for it, and never had a scaffold pass creating the slot. Fixing the fixture (demand the relation, expose more) without fixing the representation just reproduces finding 7: even with a clear ask, a non-empty state suppresses new recording. Fixing training (placeholders + position-interpreted needs) without fixing the fixture trains on a closing demand ("write the analytical brief") that the verdict metric ("ANSWER: <int>") never sees — you'd be training the wrong stage. The single highest-information-per-spend change is make the scaffold passes emit typed empty slots keyed to the structural inventory (table slots, function-binding slots) and make the rubric condition the kind of slot on k. This attacks finding 3 (no forward process), is the precondition for the REPLACE-bias to do useful work, and is testable for ~$0 with the hand-seeded placeholder probe in the handoff (run pass 26 against a seeded REGION_SURCHARGE_CENTS table — values TBD slot). If the student fills the slot, you have your single change. If it doesn't, the representation fix is insufficient and you've spent nothing.
2. (med) Favour rubric effect, least committed. Cheapest discriminator is the handoff's control (b): same crowding probe, same model, rubric that explicitly licenses ADD for content with no home in the existing state. If recording returns → rubric effect, fixable in prompt, no training run consumed. If recording stays suppressed → run control (a) on the untrained 3B with a 25-entry state: if 3B also suppresses, it is structural (an accumulating text state is intrinsically hostile to novelty) and the architecture needs an eviction/compaction policy, not a training fix. If only the trained student suppresses and 3B does not → training artifact. Order (b) before (a) because (b) is one prompt change and (a) needs a 3B context run. The "shape-irrelevant, step not slope" result already pushes hard against the rubric hypothesis (a rubric effect should depend on what the rubric says about the existing entries, and it didn't) — so my confidence is med, not high. The structural reading is the most consistent with the data but the worst for the project, so it deserves to be the last man standing only after both cheaper readings are killed.
3. (low-med) Sound, but it moves the failure to slot-type discipline. Early passes must emit the right typed slots — a slot for the wrong table, or a slot typed <number> where the fill is <cents>, gives the late pass nothing to fill. There is no teacher signal currently for slot types (the scaffold rubric says "do not chase specifics," never "leave a slot of type T"), so the slot inventory becomes a second learning problem stacked on the first. What breaks first: the scaffold pass emitting slots that don't match what late passes see, because the scaffold sees compressed views and the late pass sees verbatim tiles — the slot's type annotation is itself a prediction about content the model hasn't read yet. Diffusion's forward process does not have this problem because the corruption is content-agnostic; here the slots must be semantically typed ahead of time, which is exactly the global-structure capability the architecture is supposed to provide. So it is a real fix for findings 1–3 but it inherits the salience gap from P7/P6: deciding what slot to leave is the same decision as deciding what to record, one pass earlier.
4. (high) Yes, hard-cap. An unenforced budget that ran to 1.92× is not a budget; it is decoration, and finding 4 shows the only verbatim exposure of the relation happened at peak state pressure — the harness made the hardest pass hardest. Policy: LRU eviction of entries not referenced by the current pass's view, with a protected minority reserved for entries the rubric marks as structural (table/function bindings) — content-addressed ops make this safe (a REPLACE that hits an evicted entry is blocked, not silently misapplied). Cost: it risks evicting the splitting operand before the closing pass if the rubric does not mark it structural, so eviction must be gated on a structural-importance flag that the scaffold pass sets. The honest version is: the harness must enforce the budget, and the rubric must tell it what not to evict; the two changes are a pair, not independent. Cheapest first step is just enforce the cap with naive LRU and re-run the short battery — if RECORD improves, the cap was load-bearing; if RECORD worsens, the wrong things got evicted and you need the structural-protect flag.
5. (high) (c) no training — rebuild the fixture and re-measure first. Training spend now is paying to learn on top of three known confounds at once: the scoring demand (ANSWER: 0× in training, closing rows excluded from the health gate), the missing relation in the component attribution, and finding 7's suppression effect with no discriminator run. Any trained result would be uninterpretable — if the student passes, was it the placeholder fix, the fixture fix, the budget fix, or noise? You cannot attribute a single training run across three simultaneous changes under the project's own forcing-line discipline. The honest sequence is: (i) run the two $0 suppression discriminators, (ii) run the placeholder hand-seed probe, (iii) enforce the budget and re-run the battery, (iv) only then commit one training run with exactly one lever changed. The 3B-is-enough-untrained result (finding 5) is also a reason to delay training: 3B untrained clears the compose primitive at 9/10, so the compose joint is not the blocker — RECORD is, and RECORD is the thing the no-training experiments above measure directly. Spending the one training run before the no-training measurements are in is the project's largest current risk.
 6. (med) Legitimate but it changes the claim. "Answer from the state alone, source discarded" is satisfied trivially by a retrieval step over the state — the state is on disk, the source is not. But this concedes the architecture's reason to exist: the whole bet was that the state carries bindings a sequential bounded reader cannot, and a query-aware retrieve-then-compose step is just RAG over the model's own notes. If BM25-over-state closes the gap, you have not shown pass-conditioned reading works; you have shown RAG works and pass-conditioned reading is one (expensive) way to build the RAG store. The oracle-filtered version making the 1.5B produce 2353 is the tell: it means the information is in the state and the model cannot find it unaided — that is a retrieval failure dressed as a composition success. Legitimate as a diagnostic (it localizes the failure: state holds the operands, the close cannot surface them); not legitimate as the claim. If you adopt it, restate the claim as "pass-conditioned reading builds a queryable memory," and acknowledge that this collapses into the RAG-tie P4 already found.
 7. (med) Adopt the rule already sketched in the handoff obs.4: an anomaly counts as latent capability only when the mechanism reproduces under ablation, not when the string appears. Concretely, a sporadic success is noise until you can show it survives (a) a temperature swap, (b) removal of the helper that may have carried the answer, (c) replacement with a distractor of matched surface form. The distilled probe qualifies (same model, same state, distractors removed → 2353); the n=1 cross-family composes do not (one draw each, no ablation). The bright line: one draw is evidence of possibility, never of capability; capability requires a rate. The project's own CONSOLIDATED-FINDINGS already states this (D24, rate-not-instance). The discipline is correct; the failure mode is forgetting it when a 1/10 result is flattering (the four withdrawn hypotheses this session all came from over-reading a single draw).
 8. (high) One internal inconsistency and one convenience flag. Inconsistency: finding 7's "0 entries → 7/10 relation emitted, 25 entries → 0/10" is presented as a step at the first non-empty level, but the table also shows 50/0, 100/0, 155/2 — n=10 only at the 0 and 25 rows; the 50/100/155 cells appear to be single-digit draws (0, 0, 2) with no /10 denominator stated. You cannot call "step not slope" from one well-sampled point (25) and three under-sampled points. The step claim is over-read on n; what the data supports is "any non-empty state strongly suppresses," not "the suppression is discontinuous." The handoff's own caveat (the 0-entry 7/10 measures attention, not successful recording, because the ops would be blocked by the guard) makes the 0-vs-25 contrast not even an apples-to-apples comparison — 7/10 is a different quantity than the other cells. Convenience: finding 5's "3B untrained clears the 5/10 bar" is convenient because the bar was pre-registered for a trained student; clearing it untrained tells you the compose primitive is easy, not that the architecture works. Stating it as "scale-limited" without flagging that the bar was not set for this comparison is a small frame-slip. Everything else checks out against the archives; the four withdrawn hypotheses were honestly withdrawn.
 9. (high) Strongest honest claim today: on a single 64,671-token codebase, an untrained 7B and a 3B can compose a split-fact answer (2353) from a seeded state that holds both operands, at 10/10 and 9/10 respectively, but neither can record those operands into that state from a scheduled multi-pass read end-to-end (0/4 and 0/5); the trained 1.5B student composes 1/10 and records 0/10 end-to-end. This is a real and specific finding: composition is not the bottleneck, recording under accumulating state is. What is missing for it to matter: a single end-to-end run where the operands are recorded by the schedule, not seeded by the harness, and survive to the close. Until that exists, the project has a clean negative (the joint that blocks is identified) but no positive. Nobody outside the project cares that the negative is well-attributed; they care whether the thing works. The missing item is one successful recorded-then-composed chain, pre-registered, with the ablation rule from Q7 applied.
10. (high) Steelman: the fixture tests a relation no prompt ever asked for, shown once in 31 passes at peak state pressure; the training data was filtered by a length gate that silently dropped 52% of the verbatim rows; the closing metric (ANSWER:) appears 0× in training while every closing row demands a brief; the component attribution tracked 2 of 3 required facts; the budget was advisory and overrun 1.92×; the relevance control was uninterpretable; and four successive interpretations were retracted in one session. Each defect alone is fixable; together they mean the run produced a number (0/10) that is the product of the setup, not of the architecture. Patching one defect at a time under a pre-reg bar is the sunk-cost fallacy with discipline: each patch is a new instantiation, a new bar, a new gauntlet, and the maintainer's time is the binding constraint. A rebuild lets you design the fixture, the scoring demand, the scaffold-with-placeholders, the enforced budget, and the component attribution (including the relation) as one coherent object before any teacher call, instead of discovering the incoherence after a 5,730-row generation. The diffusion correspondence, if taken seriously, requires a rebuild — the forward process (gold→placeholders) is a different data-generation pipeline, not a patch to teacher-rollout distillation. Do I buy it: partially. The data pipeline is patchable — adding the relation to the component tracker, enforcing the budget, re-rendering with position-interpreted needs are each contained changes and the teacher trajectories are reusable for the unaffected stages. The fixture is rebuild-worthy on its own merits (the unfairness is real). But a full from-scratch rebuild risks the opposite failure: re-deriving everything means re-discovering the defects you already catalogued, at maintainer-time cost the project cannot afford on one rented run. Patch the data pipeline and the budget; rebuild the fixture; do not rebuild the pipeline wholesale until the placeholder probe and the suppression discriminators return negative. The diffusion framing, if it survives its own falsification (Q11c), is the one thing that would indeed justify a pipeline rebuild — but commit to that after the cheap test, not before.
11. (low-med) (a) Post-hoc metaphor dressed as correspondence. The constructed forward process (blank gold values into typed placeholders) is not the inverse of compression. Diffusion's forward process is a known, content-agnostic, stochastic corruption (Gaussian noise / Bernoulli mask) whose sufficient statistic is the noise level; the reverse learns p(x₀|xₜ,t) because t is a sufficient statistic for how corrupted the sample is. Compression's corruption is deterministic and semantically selective — the compressor decides what to keep based on content — so k/K is not a sufficient statistic for the state's corruption: two states at the same k can be corrupted in completely different ways depending on what the compressor deemed foreground. A reverse process conditioned on k alone cannot learn to invert a corruption it cannot characterize at that k. This is the graded difference from masked diffusion LLMs (Mask-Predict, MDLM, DiffuLLM): there the mask is uniform-random, so t = fraction-masked is sufficient; here the "mask" is compressor-chosen, so it isn't. The genuinely different thing here is content-addressed typed placeholders as the discrete corruption target, but that difference breaks the learnability condition, it doesn't satisfy it. To make the correspondence real you would need the corruption to be content-agnostic (random value-blanking, compressor-independent), which throws away the one thing pass-conditioned reading claims to add (compressor-chosen salience). (b) Yes, the Phase-A/Phase-B mixture breaks the framing and they should be separated. Phase-A (granularity refinement over the whole document) is diffusion-like and admits a forward/reverse on one object. Phase-B (sequential coverage of different regions) is filtering-like (the handoff's own word, matching the Bayesian-filtering spine in DISCO-NATIVE-ARCHITECTURE) and its "corruption" is spatial absence, not noise — a Phase-B pass is not a noisier version of the eventual full read, it is a partial read. Conflating them under one k/K makes the timestep meaningless: at Phase-B passes, k encodes where you are reading, not how corrupted the whole is. Treat Phase-A and Phase-B as two different mechanisms with two different schedulers; the current single-conditioning conflates them and the model cannot tell whether k=15 means "noisy global view" or "clean local tile." (c) Cheapest falsification: hand-construct, for one document, gold states at five k levels by blanking the gold final state into typed placeholders hierarchy-style (23: 988→23: <cents>→REGION_SURCHARGE_CENTS: <table>→absent). Train LoRA on noise-level → next-noise-level pairs only (no teacher rollout). If the trained model, at test time, cannot reconstruct a gold-equivalent final state from the most-blanked level on a held-out document better than an untrained 3B prompted to do the same, the correspondence is falsified — the reverse process is not learnable from constructed pairs under k-conditioning alone. This costs one short LoRA run, no teacher API, and is path-independent by construction as claimed. Run it before rebuilding the pipeline around the correspondence.

---

cantus-qoder

 ▪ 1. Fix the fixture first — but the cheapest high-information change is prompt-only: rewrite the recording rubric to demand relations/behaviour ("record what functions compute, not just names") plus an ANSWER: <int> close, then re-run the untrained-3B
   chain battery. No training, no API teacher, one 1080 Ti weekend. It directly discriminates "correct editorial omission" from "capability failure" (your known weakness #1), and every other decision (Q3–Q5) depends on that answer. Fixing training data
   before knowing whether the rubric alone unblocks RECORD risks burning the one A5000 run on a solved problem. Also add ≥3 exposures of the function body at varied passes — one exposure at peak state pressure is a confound you can delete for free.
   Confidence: high.

   2. I favour the rubric×protocol interaction: "integrate into the entries it belongs to" plus REPLACE-bias gives the model no licensed action for genuinely new content when entries exist. Evidence: it's a step, not a slope (capacity/attention dilution
   predicts a slope over 25→155), and schema doesn't matter (content interference predicts schema sensitivity). Cheapest discriminating experiment: a 2×2, ~40 inference samples — {original rubric, rubric with explicit "ADD new entries for content matching
    no entry"} × {trained 1.5B, untrained 3B}, all with the same 25-entry state. If the ADD-licensed rubric restores recording in both models → rubric. If only untrained recovers → training artifact. If neither → structural. Critically, score
   guard-passing recorded ops in both cells — your empty-state 7/10 counts malformed REPLACEs, so the headline 7-vs-0 contrast currently compares incompatible metrics and may partly be an artifact. Confidence: medium-high.

   3. Sound direction, but it relocates the failure to slot selection: early compressed views name hundreds of candidate entities; emitting typed slots for all of them blows a budget you already overran 1.92× by pass 7. What breaks first: slot explosion,
   then slot-targeting (the pass-26 filler must match a slot created 20 passes earlier from a name-only mention — a harder content-addressing problem than REPLACE-by-quote). And it does nothing about finding 7: the fill pass still has to attend to new
   content under a full state. Adopt it only with a slot budget and a priority rule, and test slot-filling in isolation (seeded slots, one fill pass) before end-to-end. Confidence: medium.

   4. Yes — an unenforced budget falsifies the project's core "bounded state" premise; at 69%-of-input the architecture has degenerated into re-reading its own notes. But do not let the harness evict heuristically (FIFO/recency would silently delete
   exactly the split operands the task needs). Instead: harness rejects any pass output leaving state > budget and re-prompts demanding REMOVE/merge ops until compliant. Cost: extra inference tokens per over-budget pass, plus you'll now measure
   wrong-eviction — which is the honest failure mode you want visible, not hidden. Confidence: high (cap), medium (policy).

   5. (c), staged into (a). Training on repaired data before demonstrating the repaired fixture is passable-by-untrained-3B repeats the exact error that produced the 0/10 "architecture gate." Sequence: repair fixture/rubric → re-run untrained 3B → if
   RECORD still fails (finding 6 predicts it does), spend the run on 3B, repaired data, RECORD-targeted — composition is already free at 3B (9/10), so training should buy the one joint scale doesn't. Never (b): trained 1.5B composes 1/10 on a seeded
   state; you'd be training against a scale wall (Fisher p≈5×10⁻⁴ against you). Confidence: high.

   6. Legitimate. The constraint is "state alone, source discarded"; retrieval over the state uses no forbidden information — it's a working-memory aid at close, and human readers do the equivalent. But two caveats: (i) declare it in the pre-registration,
    since it changes what the trained close must learn; (ii) your oracle version smuggles in query-relevance the honest version must earn — run BM25-with-the-actual-question, not oracle filtering, before claiming it. Also note what the oracle result
   implies (see 8). Confidence: high.

   7. Rule: a sporadic success is evidence of latent capability only if (a) it replicates at pre-registered n with rate exceeding a measured floor at exact-binomial p<0.01 (Bonferroni over however many "surprises" you chased that week), and (b) the
   mechanism is visible in artifacts (the fact is literally in the state), not inferred from generations. Your own audit record supports this split: artifact-read claims held 4/4, probe-inferred claims died 4/4. One-off successes at n≤10 with unknown
   floor are noise by default. Confidence: high.

   8. Three flags. (i) Findings 1 and 6 are in tension: if 0/28 states recorded the relation, yet oracle-filtered 1.5B produces 2353 from state alone, then the relation was inferable from entry names/context — so "the scored chain was unpassable" is
   overstated. It was unpassable-by-recording but apparently passable-by-inference-at-close. That weakens the "any model answering 988 is reasoning correctly" absolution. (ii) The 7/10 empty-state cell measures attention, not recording (you flag this, but
    the table still headlines it as a suppression contrast — it isn't one yet). (iii) Fisher p≈5×10⁻⁴ for 9/10 vs 1/10 is the one-sided value (two-sided ≈1.1×10⁻³); fine, but say which. Minor: 28 archived states from "10 draws per arm" arithmetic is
   unexplained. Percentages and token arithmetic check out. Confidence: high on (i), high on (iii).

   9. Honest claim today: "In an edit-op bounded-state protocol, compose-from-clean-state is solved by untrained ≥3B models (9–10/10) while relation-recording fails at all tested scales — and our audit shows the recording failure is at least partly
   manufactured by our own rubric, training data, and single-exposure fixture." That's a real, localized negative result with a clean decomposition. What's missing for anyone to care: one head-to-head on a real long document against a cheap baseline (RAG
   or hierarchical summary) on a query the baseline structurally can't answer. Everything so far is internal to the harness. Confidence: high.

   10. Steelman: every layer encodes the wrong objective — rubric wants values not relations, close wants briefs not answers, the health check exempts the only scored stage, teacher policy is 61% lossy, budget is fiction. Patching means simultaneously
   changing prompts, teacher policy, scoring, and harness; at that point nothing survives but the scheduler, and the constructed-forward-process idea eliminates the teacher (and its API cost) entirely — rebuilding is cheaper than patching. Verdict: I buy
   it for the training-data pipeline (teacher-trajectory cloning is misaligned and expensive; kill it), but not for the fixture — its planted split-fact structure is your only calibrated instrument, and findings 1–2 are prompt bugs, not design rot.
   Rebuild data, patch fixture. Confidence: medium-high.

   11. Mostly post-hoc metaphor with one load-bearing idea. (a) Diffusion's reverse process is learnable because the forward process is known, simple, stochastic, and — crucially — the inference-time input at step k is exactly a sample from that forward
   process. Compression is deterministic and semantically selective, so "noise level" isn't a controlled distribution. The constructed forward process (blanking gold states) is fine as supervised data augmentation, but then it's infilling/curriculum
   training, not diffusion — and it has a concrete failure diffusion doesn't: exposure bias. Real rollout states will drift off the constructed-state manifold (yours already did: monotone accretion, 725→7,692 tok, zero placeholders), and nothing
   guarantees the model can denoise states it actually produces. (b) Yes, the mixture breaks it: Phase-A (granularity refinement) is timestep-like; Phase-B (sequential region coverage) is a scan — an orthogonal axis masked diffusion has no analogue for.
   Condition on them separately or the k-signal is confounded. (c) Cheapest falsifier, no training: train a trivial classifier to distinguish real archived pass-k states from constructed noise-level-k states. If they're near-perfectly separable, the
   constructed forward process does not model the trajectory the model traverses, and the correspondence is decorative. Versus masked-diffusion LLMs (D3PM/SEDD/LLaDA-style): the genuine difference is a persistent external text state edited by
   content-addressed ops rather than a token canvas — but that difference is precisely what forfeits diffusion's guarantees. Confidence: medium.
   
---

inkling-opencode

 1. Fix the fixture first (high). Finding 1 makes the chain unscorable — no training fixes an untestable joint. Repair the close demand (ANSWER: inserted in rubric), expose the relation in ≥2 non-co-occurring tiles, add the v2 positive control (STAGE_ORDER), and pre-register architecture-vs-model failure criteria. That gives the most info/spend. Training repair (placeholders, rubric) is second; representation (hard cap) is third.
 2. Structural/state-pressure (med). Not rubric-specific: 0/10 under different schema (control 25 entries), step-like suppression, shape-independent. Cheapest discrimination: same 25-entry seed, vary only rubric ("record this formula" vs "update state" vs neutral). If rates stay 0/10, it's state pressure, not rubric.
 3. Moves failure, doesn't solve it (high). With REPLACE-biased protocol and no scaffold slots (finding 3: 1.8% placeholders), typed empty slots break first: nothing exists to REPLACE, so the model either emits malformed ADDs or ignores them. First failure: no mechanism to populate slots without training the ADD behavior the rubric suppresses.
 4. Yes — hard-cap with forced LRU eviction (med). Policy: evict oldest entry (not newest), with content-addressed guard (D31). Cost: harness-only; exposes whether retention fails are model-level or overflow-level. Given 1.92× and pass-26 pressure (69% input), a 4000-token cap is the only honest enforcement.
 5. (c) No training — rebuild fixture and re-measure first (high). Finding 1 (unscorable chain), 2 (close excluded from health gate), 3 (no scaffold data to learn), 5 (3B untrained already clears 9/10 on clean compose — joint isn't scale-blocked), 6 (3B doesn't fix recording). Training a broken pipeline teaches broken behavior.
 6. Not legitimate (high). It violates "source discarded, answer from state alone." The user confirms an oracle-filtered retrieval makes 1.5B hit 2353 — that's RAG, not this architecture. If adopted, the claim collapses to a known result; the architecture question becomes moot.
 7. Rule: n≥5 pre-registered with mechanism (med). Isolated draws (e.g., 2/4 at P7 GATE 3) = noise unless replicable and explained structurally (e.g., 3B f16 9/10 is scale, not noise). Adopt: if success rate > floor + non-overlapping CI and mechanism explained → capability; else noise. Finding 7's empty-state 7/10 is attention, not operation — don't count it.
 8. Inconsistency: "16-model gauntlet" (high). Docs show 7 model families (Qwen-72B, Llama-70B, Mistral-24B, gpt4omini, novalite, commandr, haiku45); 16 is reviewer count in gauntlet arbitration, not model count. Also: diffusion framing is post-hoc metaphor — disk text ≠ continuous latent; "corruption" is deterministic semantic selection, not random noise. Finding 7's 7/10 empty-state is malformed ops (user admits this), so it's not valid recording evidence.
 9. Honest claim (high): "Bounded-state pass-conditioned reading preserves split-fact operands and composes them correctly on a seeded clean state (GATE 3: 4/4), but the untrained floor fails the full chain end-to-end (0/4) with identifiable, non-generic failures (salience gap, edit-op clobber, missing relational binding)." What's missing: a trained model tested under a scorable, positive-controlled fixture with pre-registered architecture-vs-model failure criteria.
10. Partially buy (med). Fixture unfairness (one exposure, peak-state formula, never-requested relation) and pipeline exclusion of closing/placeholders justify major repair. But "unsalvageable" is wrong: component isolation (E0 signal-carrying, P3 rubric separation, GATE 3 4/4) shows the architecture isn't broken. Drop the diffusion metaphor; keep the schedule but separate Phase-A (global compression) and Phase-B (local coverage); adopt D31/D32 harness fixes; don't rebuild from scratch.
11. (a) Breaks the analogy (high). Diffusion corruption is random/independent; compression is deterministic and semantically selective — what survives is chosen by the compressor, not sampled. The constructed pairs work as curriculum data but don't inherit diffusion's learnability proof. (b) Yes, breaks framing — Phase-A (granularity over whole doc) and Phase-B (sequential tile coverage) mix two time-axes; k/K means different things. Separate them. (c) Falsify: train 3B on constructed pairs, test on noisy/place-holder states (low cost). If it fails to improve over 9/10 clean-state baseline, the process isn't learnable. Difference from masked diffusion LLMs (D3PM/SEDD): those use continuous/token embeddings with random masking; here state is discrete text edited symbolically, and "noise" is structured blanking, not stochastic.

---

gpt-5.6-terra-codex

• 1. (High) Fix the fixture/task contract first. The gate cannot diagnose representation when training never requires relations or the scored closing behavior. Highest-value change: pre-register a relation-positive-control fixture where a generic code-
     behavior need class requires executable contracts, the relation is score-required in state, and the close task matches training. Run it before training. Do not call the current 0/10 an architecture result.

  2. (Med) I favor a rubric/state-presence interaction, not a demonstrated structural property. “Varying only state” also changes prompt length, token positions, valid edit targets, and the model’s perceived task. Run a 2×2×2 probe: trained/base ×
     empty/25-entry equal-length state × current/explicit-ADD rubric. Give every condition a valid new relation slot; score proposed relation and successfully applied relation separately. A training artifact is model-specific; rubric effect is
     instruction-specific; structural suppression survives both.

  3. **(Med1. (High) Fix the fixture/task contract first. This gate cannot diagnose representation when it neither supervises nor scores the required relation and trains a different close behavior. Highest-value change: preregister a relation-positive-
     control fixture whose generic code rubric requires executable contracts (input→output relation), uses a train-matched operational close, and exposes the function before state pressure. Run it before another training job.

  4. (Med) I favour a rubric/state-prompt interaction, not yet a structural conclusion. “Varying only state” also varies length, position, valid REPLACE targets, and the model’s perceived task. Cheapest discriminator: trained versus base × empty versus
     25-entry state × “ADD this relation explicitly” versus current integrate wording, length-matched with inert padding and valid ADDs. Score proposed and successfully applied relation ops separately.

  5. (High) Typed slots are sound only if the harness supplies a small, declared schema and field-level updates. If the model must invent slots, it merely moves RECORD earlier: it will omit the needed slot, create a generic bloated inventory, or misbind a
     value to a plausible slot. The first failure is slot selection, not filling. Textual placeholders plus REPLACE are a fragile substitute for typed relations.

  6. (High) Hard-cap the state in the harness. The current result is not a bounded-state result. Do not silently force arbitrary eviction: reject over-cap writes and require an explicit compact/remove operation. If automatic eviction is unavoidable, evict
     duplicate summaries first, then summaries fully supported by retained atomic facts; reserve space for exact values, relations, provenance, and unresolved slots. Cost: this becomes a substantive memory-policy component and may reveal that the claimed
     capacity was never achievable.

  7. (High) (c), no training yet. The current training target, health check, budget, and fixture do not establish a meaningful training failure or architecture failure. A 3B repaired-data run is the conditional next run after the rebuilt diagnostic passes
     its positive controls. Training 1.5B again is least defensible; the clean-state probe says its compose ceiling is probably too low.

  8. (High) Yes. Retrieval over the final state is legitimate: the source is still discarded. But it is a new system component, not a harmless close prompt. Use a non-oracle retriever over typed/provenanced state entries, preregister top-k, and compare it
     with the same cap and compute. Oracle filtering establishes only conditional compose capability; it says nothing about retrieval, recording, or relation preservation.

  9. (High) One provenance-clean success proves possibility, not reliable capability. Call it an anecdote unless it replicates on fresh fixture seeds specified before inspection. Adopt three labels: valid isolated success; replicated mechanism (same
     intervention succeeds on held-out variants); demonstrated capability (preregistered rate and paired improvement over floor). Post-hoc probes may generate hypotheses, never upgrade the gate result.

  10. (High) Several problems:
      - The reported Fisher (p\approx5\times10^{-4}) for 9/10 versus 1/10 is one-sided; two-sided Fisher is about (1.1\times10^{-3}).
      - The pre-registered ≥5/10 bar is for end-to-end success, not seeded compose. A 3B seeded-compose result does not clear that bar.
      - “988 is reasoning correctly” without the function relation is wrong. The state underdetermines the function’s result; 988 is an unsupported guess.
      - The primary gate would accept operands plus a visible sum without requiring that the function relation was retained. That contradicts “answer from final state alone.”
      - The health-check code explicitly excludes stage == "closing". Thus it cannot certify the only behavior the verdict scores.
      - “69% of the model input” needs its denominator: 7,692 is 31% of the documented 24,576-token context.
      - Finding 7’s empty-state condition produces malformed blocked REPLACEs, so it measures generated attention, not recording. It cannot support a claim about successful state updates.

  11. (High) Today: “LoRA improved this 1.5B model’s edit following and some recording on one synthetic setup, but did not yield reliable final-state question answering.” Missing: a preregistered, goal-aligned long-session nuance benchmark against
     credible long-context and retrieval baselines.

  12. (Med) Steelman: rebuild because rolling teacher text states create exposure bias; the state is uncapped, flat, untyped, and hard to retrieve; supervision omits the critical behavior; and the synthetic chain is not the stated goal. Patching risks an
     accumulating bespoke system. I buy rebuilding the experimental pipeline and state representation, not discarding the scheduling hypothesis. Retain the schedule as an ablation, but treat the rebuilt system as a new instantiation.

  13. (High) This is mostly a metaphor, not a diffusion correspondence. True diffusion specifies a forward corruption distribution and learns a reverse conditional over that distribution; masked-diffusion LMs such as LLaDA
     (https://arxiv.org/abs/2502.09992) do this with token masking, while Diffusion-LM (https://arxiv.org/abs/2205.14217) uses continuous noise. Your compression is semantic, deterministic, and observation-dependent; disk text is external memory, not a
     latent variable.

  (a) Blanking gold states can produce useful denoising/curriculum labels, but it is not path-independent training for the deployed system. It does not train recovery from its own omissions, wrong bindings, excess state, or views lacking the blanked fact.
  It works only if corrupted-state-plus-remaining-views identifies the gold field.

  (b) Yes, separate Phase-A global refinement from Phase-B regional coverage. The latter is online state estimation, not progressive denoising. k/K currently confounds noise level, phase, and location coverage.

  (c) Cheapest falsifier: compute collision/conditional ambiguity on generated data—whether identical (corrupted state, remaining scheduled views, phase) ever require different exact gold fields. Any nontrivial ambiguity for required slots falsifies
  deterministic reverse recovery. The genuinely different idea is persistent external state updated across a document schedule; that is closer to learned memory than diffusion.