# Handoff — 2026-08-06 (publication day, and the density ladder ran)

## Read these first, in order

1. **`experiments/e2prime/SIGNAL-DENSITY-RESULTS.md`** — the session's experiment. P1i, the
   signal-density ladder: design committed before the run, replicated on fresh seeds.
2. `docs/COMMIT-MAP-2026-08-06.md` — the identity rewrite that preceded publication; decodes the
   old commit hashes cited in earlier docs.
3. `docs/HANDOFF-2026-08-05.md` — the pyramid/confirm record this session published and built on.

## Part 1 — the repo went public, whole

Everything through 08-05 was committed (seven topical commits reconstructing the 07-29→08-05
arc), the 62-commit unpushed history was rewritten to the public identity (author/committer
fields; one committed line that quoted the two private strings was rephrased in-history; dates
preserved to the second; map in `COMMIT-MAP-2026-08-06.md`), the branch was pushed, and **main
was fast-forwarded and pushed — the full program is now the default public view**, per the
maintainer: the process is in the open, and being upfront about it matters more than polish.

Front-door changes, all live on main:

- **README scope section** — the framing correction, now load-bearing: DiSCo targets any model
  at any window; the use case is multi-million-token sessions no window contains; the small
  local models are the compute-bound test rig; the honest baselines at target scale are rolling
  compaction / hierarchical merging / retrieval. The economics drift ("small and cheap is the
  point") is dead on the front door — it was never the design intent.
- **README "How this work is actually done"** — process transparency in the maintainer's voice:
  AI-executed, human-directed, with the controls named and the reason they exist (both parties
  can be wrong; both have been).
- **README status update + PUBLIC-SUMMARY addendum** — the record since the consolidated
  findings, caveats stated.
- **`docs/DISCO-README-SNAPSHOT-2026-08-06.md`** — the parent proposal vendored verbatim, so the
  spec this repo tests is greppable in this repo. The schedule discrepancy went six weeks
  undetected partly because it wasn't.
- **CONFIRM-PREREG provenance addendum + standing rule:** a pre-registration counts only once
  committed AND pushed before the run it registers. The 08-02 prereg's ordering evidence is
  filesystem mtimes (supportive, but weaker than a pushed commit) — stated inside the document.

## Part 2 — P1i, the signal-density ladder (HANDOFF-2026-08-05 next-step #1)

The queried-table section (263 tok, `23: 988` one row of 24) byte-identical in five cells; only
the surroundings vary. Pinned no-home s25 state, verbatim rubric, k=12 honest, student, strict
guard. Design + readings committed pre-run (`58a59ee`); fresh-seed replication (21-30, never
used) run before anything was written up.

| signal share | pooled applied mapping /20 |
|---|---|
| 10.0% (the real tile) | **1** |
| 35.5% | **7** |
| 66.4% | **8** |
| 92.3% | **9** |
| 9.8% prose-padded control | 4 |

**Density is the constraint, and the step is at the bottom:** 10%→35% buys 1/20→7/20 (trend
across the ladder p≈0.005; d90 vs base p≈0.008 two-sided). Above ~35% the rungs do not separate.
The prose-vs-code filler discriminator did not resolve (4/20, between poles, n.s. against both).
Composition caps at ~half: the d90 misses, all hand-read, are grinding loops (13-14
byte-identical REPLACEs of S1) and name-or-range-without-rows — the training/demand residue no
fixture composition removes. Zero fabrication in 100 draws. Dilution also feeds the grinder:
long-view cells emit 2-4x the ops, half to two-thirds byte-identical no-ops.

**The fixture rebuild now has both its numbers:** total distinctive content > V (08-05), and
≥ ~35% distinctive share within each decisive tile (P1i). `author_pyramid_views.py
--budget-audit` checks the first; check the second per-tile before authoring.

## Part 3 — the alternative arrived the same day: G2's premise check ran, and PASSES

The maintainer's `claude -p` alternative is the remote host's OAuth pool over ssh (the D30
path, `ClaudePClient(ssh_host=...)`, host via `PCA_CLAUDE_SSH_HOST`, never in the repo). Two
API rungs were added to the G4 ladder (design committed pre-run, `5530f71`) and run on the
identical distilled evidence set, n=10 each, **every draw hand-read**:

| rung | integrated (2) | partial (1) | flattened (0) |
|---|---|---|---|
| locals 1.5B-14B (07-29) | 0/10 every rung | — | most |
| haiku_remote | **6/10** | 4/10 | **0/10** |
| sonnet_remote (claude-sonnet-5) | **10/10** | 0/10 | **0/10** |

Every sonnet draw names the displacement pattern and imposes an explicit bound; several
recover the ordering leg (checks-before-work) from timestamps alone. **The §6a zeros now mean
"small models can't make the call," not "the arc is too subtle" — the M-NL arms move to API
models per R2.** Full G2 (whole authored fixture) still owed. ⚠️ The mechanical pre-filter
printed 1/10 provisional 2s at BOTH rungs against 6/10 and 10/10 hand-read — it misses
API-diction phrasings ("quarantine", "batch into one slot", "crowding out delivery"). It was
always labelled a reading aid; this is the proof. Never quote its numbers. Details + two
fixture-authoring notes: `MNL-TYPE-A-FIXTURE-DRAFT.md` §6b.

## Where to go next

1. **Phase-2 fixture rebuild** — both requirements are now numbers; the demand repairs
   (ledger 1-8, esp. 1a/1b) ride along. Then F2 per `docs/STACK-SEARCH-PLAN.md` (the
   maintainer signs the §6 bar first).
2. **F1 joint battery sweep** — specified in the plan, needs ~an hour of plumbing
   (`retention_probe.py --model`, the capability-map summarizer), then one-two overnight
   batches.
3. **M-NL fixture bulk** — authoring is now the gating step (maintainer authors/approves);
   the premise is measured, the arms are API-bound, §6b carries the authoring notes.
4. **P1c** (enforced budget) — still the open probe on the board.
5. Baseline arms for the rebuilt fixture are rolling compaction / hierarchical merge / RAG at
   matched budget — not a dense read (scope section, and the P4 precedent).

## State of the tree

Committed and pushed this session (all on `e2prime-prep` = `main`): the seven reconstruction
commits, the identity rewrite, snapshot + commit map, front-door updates, the process note, the
P1i design commit, and the P1i results + this handoff. Probe artifacts live under
`runs/stageA_chain_gate/_probe_p1/p1i_student_s01-10.json` and `.../p1i_student_s21-30.json`
(runs/ is gitignored, as always). `python -m pca.test_editops` passes. Local
`refs/original/*` still holds the pre-rewrite history as a never-pushed safety net — delete when
comfortable.

**Environment unchanged:** `PYTHONIOENCODING=utf-8` mandatory; system `python` 3.14, no `uv` on
this box; student f16 serves on the 1080 Ti with full offload; ~9 s/draw single-pass cells.
